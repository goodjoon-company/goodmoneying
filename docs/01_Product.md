# 제품 개발 사양(PRD, Product Requirements Document)

Status: Accepted
Last Updated: 2026-07-16

## 문서 역할과 사용법

이 문서는 goodmoneying의 제품 목표, 범위, 정책, 요구사항, 마일스톤(Milestone), 사용자 화면을 정의하는 단일 기준(source of truth)이다. 현재 구현 사실과 앞으로의 제품 우선순위를 분리하며, 설계·구현·검증 상세는 [아키텍처 개발 사양](02_Architecture.md), [계약 기준](contracts/README.md), GitHub Issue, `docs/Test/`, `docs/History/`에서 관리한다.

현재 기준은 GitHub Issue #4의 제품 의도 결정과 Issue #5의 구현 산출물 분류를 반영한다. 브레인스토밍(Brainstorming) 또는 과거 계획과 이 문서가 다르면 이 문서를 우선한다.

개발자는 구현 전에 `제품 목표`와 `현재 제품화 범위`를 읽고, 요구사항 ID와 상태를 확인한 뒤 관련 GitHub Issue·아키텍처·계약으로 이동한다. 기능이 완료됐다고 판단하려면 요구사항의 제품 결과와 `다음 제품 단계`의 완료 판단을 모두 충족해야 한다.

## 사양 요약

| 항목 | 현재 기준 |
|---|---|
| 제품 대상 | 개인 단일 사용자의 업비트 KRW 코인 분석 작업대 |
| 현재 사용자 결과 | 관심 코인 선택, 신뢰도 확인, 차트·거래량·고정 기술 지표·현재 시장 상태 분석 |
| 데이터 기반 | 실시간 수집, Backfill, 1분·일봉 원천봉, 분석용 집계 봉 |
| 운영 경계 | 운영 상태·Backfill·시스템 관리는 사용자 가치 화면을 지지하는 내부 도구 |
| 명시적 비범위 | 투자 추천·자동 주문, 주식, 다중 사용자, 권한·과금, 뉴스·LLM |
| 실행·검증 추적 | GitHub Issue, `docs/Test/`, `docs/History/` |

## 제품 원칙

- goodmoneying은 운영 콘솔이 아니라 개인 투자 의사결정을 돕는 데이터 기반 작업대다.
- 운영 화면은 데이터 신뢰성을 확인하는 내부 도구다. 사용자 가치의 최종 화면은 관심 대상 탐색, 비교, 분석, 전략 실험이다.
- 현재 제품은 업비트 KRW 코인 분석에만 집중한다. 주식은 별도 제품 결정 전까지 화면·수집·분석 범위에 넣지 않는다.
- 가까운 로드맵(roadmap)은 1~2단계만 구체화하고, 이후 단계는 진입 조건을 확인하는 결정 게이트로 둔다.
- 구현된 사실, 확정된 제품 결정, 후보 아이디어, 의도적으로 보류한 범위를 서로 다른 상태로 관리한다.
- "나중에 쓸 수도 있는 데이터"보다 다음 투자 판단에 실제로 쓰는 데이터와 설명 가능성을 우선한다.
- M1~M3에서 정의한 최소 기능 제품(MVP, Minimum Viable Product) 구현은 이미 완료됐다. 현재 로드맵은 초기 구현 이후 제품화(Post-MVP Productization)와 사용자 가치 검증을 다룬다.
- 개인 단일 사용자 제품을 전제로 하며 투자 수익 보장이나 법적 의미의 매수·매도 추천을 목표로 하지 않는다.

## 제품 목표

### 현재 목표

다음 1개월의 목표는 운영 안정화 자체가 아니라, 신뢰 가능한 업비트(Upbit) 데이터 기반 위에서 사용자가 전체 코인 목록에서 관심 코인을 고르고 한 코인을 실시간으로 분석할 수 있는 작업대로 이동하는 것이다.

현재 제품화 단계의 성공은 다음 결과로 판단한다.

1. 사용자가 전체 수집 후보군에서 관심 코인을 선택하고 관심목록을 정한다.
2. 사용자가 관심 코인 하나의 최대 1년 일봉과 최대 3년 범위 차트를 거래량·기술 지표와 함께 읽는다.
3. 사용자가 차트 시간 단위를 월·주·일·시·30분·10분·5분·1분으로 전환하고, 현재가·호가 요약·체결 흐름을 실시간으로 확인한다.
4. 데이터가 부족하거나 오래됐을 때 그 사실과 복구 경로를 알 수 있다.
5. 운영자는 시스템 관리 화면에서 실시간 수집, Backfill, 캔들 집계의 실제 실행 대상·데이터 유형·진행률을 확인하고, 오래된 집계가 자동 복구되는지 판단한다.

수집 성공, 커버리지(Coverage), 최신성, 결측 복구는 이미 구현된 초기 제품 기준선이다. 현재 단계의 완료는 이 기반을 사용해 위 사용자 결과를 실제로 달성하는 것으로 판단한다.

### 장기 방향

장기적으로는 코인 분석에서 검증된 사용자 가치와 데이터 모델을 바탕으로 뉴스·공시·리포트와 대규모 언어 모델(LLM, Large Language Model) 신호를 결합한 전략 실험을 검토한다. 주식, 봇(Bot) 시뮬레이션(Simulation), 모의매매(Paper Trading), 실제 자동매매(Live Trading)는 현재 제품 범위가 아니며, 코인 분석 경험을 검증한 뒤 별도 제품 결정으로만 검토한다.

이 장기 방향은 확정된 실행 계획이 아니다. 각 영역은 직전 단계에서 사용자 가치, 필요한 데이터, 비용, 리스크를 다시 확인한 뒤 별도 요구사항과 아키텍처 결정 기록(ADR, Architecture Decision Record)으로 구체화한다.

## 완료된 초기 제품 기준선

M1~M3의 업비트 수집 운영 기능은 구현과 자동화 검증을 마쳤다. 현재 작업은 아래 기능을 다시 MVP로 구현하는 것이 아니라, 제품 기반으로 유지하거나 사용자 가치 흐름에 맞게 재배치하는 단계다. 상세 근거는 GitHub Issue #5와 `docs/History/2026-07-10-P0-3-현재-구현-산출물-분류.md`에 있다.

| 기반 | 제품상 위치 | 현재 처리 |
|---|---|---|
| 업비트 수집 후보군 상위 100개와 활성 수집 대상 50개 | 신뢰 가능한 코인 데이터 기반 | 유지한다. 후보 선정과 활성 대상 계약의 신뢰성을 우선한다. |
| 관심종목 화면 | 투자 후보 탐색의 중심 화면 | 유지하고 비교·탐색 흐름을 다음 단계에서 강화한다. |
| 코인 상세 레이어 | 관심 대상 분석의 진입점 | 유지하고 운영 상세보다 투자 분석 맥락을 강화한다. |
| Backfill 관리 | 과거 데이터 준비와 결측 복구 도구 | 유지하되 별도 제품 중심 화면으로 확장하지 않는다. |
| 운영 상태 대시보드 | 데이터 신뢰성을 확인하는 내부 운영 도구 | 유지하되 투자 후보 탐색보다 앞서 확장하지 않는다. |
| 실시간 체결 히트맵 | 투자 판단 가치가 검증되지 않은 구현 | 현재 구현만 보존하고 추가 확장을 보류한다. |

호가 원천 스냅샷(Snapshot) 저장과 다중 워커·고가용성(High Availability)은 초기 제품에 포함되지 않은 미구현 범위다. 현재 필요성이 확인되지 않았으므로 호가 요약(Orderbook Summary)과 단일 워커 운영을 유지하고 각각 전략 실험·확장성 결정 게이트에서 재검토한다.

현재 런타임(Runtime) 기능 중 즉시 삭제할 제품 기능은 없다. 과거 상세 Task 문서는 Milestone 요약과 Test/History 증적으로 이관한 뒤 제거했다.

## 다음 제품 단계

| 순서 | 사용자 결과 | 주요 범위 | 진입·완료 판단 |
|---|---|---|---|
| 완료 기준선 | 업비트 데이터를 수집하고 운영자가 신뢰성을 확인한다. | 후보·활성 대상, 실시간 수집, Backfill, 운영 상태, 관심종목, 코인 상세 | M1~M3 구현과 기존 자동화 검증 증적을 유지한다. |
| P0 | 운영 도구와 사용자 가치 화면의 경계를 명확히 이해한다. | 현재 산출물 분류, 제품 기준 재작성, 아키텍처·계약·용어 정렬, 인계 | Issue #3~#9의 결정과 검증이 단일 기준 문서에 반영된다. |
| P1 | 사용자가 전체 코인 목록에서 관심 코인을 선택하고 데이터 신뢰를 이해한다. | 관심목록 선택, 데이터 품질 설명, 결측·오래된 데이터 표시, 복구 경로 | 사용자가 분석할 코인과 데이터 신뢰 여부를 판단하고 필요한 Backfill 경로를 찾을 수 있다. |
| P2 | 사용자가 관심 코인 하나를 실시간 차트로 분석한다. | 최대 3년 차트, 월·주·일·시·30분·10분·5분·1분 봉, 거래량, 기술 지표, 현재가·호가·체결 흐름, WebSocket 메시지 계약 | 대표 시나리오에서 사용자가 1년 일봉과 실시간 시장 상태를 읽고 시간 단위를 바꿔 분석한다. |
| P2.1 | 운영자가 집계 데이터의 최신성과 생성 작업을 관리한다. | 1분 원천봉 기반 5·10·30분·시·일·주·월 집계 테이블, 자동 집계 워커, 작업·대상별 진행률, 시스템 관리 화면 | 분석 조회가 집계 테이블을 우선 사용하고, 오래된 집계는 자동 작업으로 전환되며 운영자가 수집·집계 상태를 확인한다. |
| P2.2 | 개발자가 업비트 공식 인터페이스를 안전하고 설명 가능하게 검증한다. | 전체 시세 조회(Quotation) REST, 거래·자산 관리(Exchange) REST, 공개·비공개 WebSocket, 요청·응답 추적, 비파괴 안전 정책 | 공식 API 목록이 기능 그룹별 화면에서 호출·시각화되고, 실제 주문·자산 이전·입출금 없이 정상·오류 응답과 요청 제한을 자동 검증한다. |
| P3 | 전략 실험에 필요한 데이터와 화면을 결정한다. | 지표, 조건, 실험 단위, 결과 해석, 호가 원천 필요성 | 구현 전 데이터 모델과 실험 시나리오를 승인하는 결정 게이트를 통과한다. |
| Later | 주식, 뉴스, LLM, 봇, 거래로 확장할지 결정한다. | 국내·미국 주식, 뉴스·공시·리포트, LLM 신호, 시뮬레이션, 거래 | 각 영역 진입 전에 별도 제품 질문과 ADR을 승인한다. |

P0는 2026-07-11 GitHub Issue #3~#9의 기준선 고정, 제품 의도 확인, 산출물 분류, 제품·아키텍처·계약·용어 정렬과 최종 검증을 완료했다. P1·P2의 첫 실행 단위는 GitHub Issue #12이며, 코인 전용 관심목록과 WebSocket 기반 분석 경험을 함께 제공한다.

P1과 P2의 세부 기능은 각 단계의 요구사항 상세화와 GitHub Issue가 준비될 때 확정한다. 이 문서는 먼 미래 기능을 구현 일정이나 확정 Task로 해석하지 않는다.

## 현재 제품화 범위

현재 단계는 업비트 수집 MVP를 추가 구현하는 범위가 아니다. 완료된 데이터 기반을 사용해 관심 코인을 비교하고 투자 후보를 좁히는 제품 경험을 만드는 범위다.

- 업비트 수집 후보군 상위 100개와 활성 수집 대상 최대 50개, 캔들(Candle), 현재가, 거래대금, 호가 요약 수집은 완료된 기반으로 유지한다.
- 관심종목에서 현재가, 거래대금, 등락률, 기준 시각, 캔들 데이터 범위와 데이터 신뢰도를 같은 기준으로 비교한다.
- 코인 상세에서 가격 흐름과 분석에 사용한 데이터 맥락을 이해한다.
- 데이터가 부족하거나 오래됐을 때 그 상태와 Backfill 복구 경로를 알 수 있다.
- 비교·정렬·필터·상세 연결을 통해 다음에 분석할 투자 후보를 좁힌다.
- 관심종목과 코인 상세는 사용자 가치 화면으로, 운영 상태와 Backfill은 이를 지지하는 내부 도구로 구분한다.

### 제품 화면 경계

| 구분 | 화면 | 역할 |
|---|---|---|
| 사용자 가치 | 관심종목 | 관심 코인을 같은 기준으로 비교하고 후보를 좁힌다. |
| 사용자 가치 | 코인 상세 | 선택한 코인의 가격 흐름과 데이터 맥락을 분석한다. |
| 내부 운영 | 운영 상태 | 수집 파이프라인과 데이터 신뢰성을 진단한다. |
| 내부 운영 | Backfill 관리 | 과거 데이터 준비, 결측 복구, 작업 제어를 수행한다. |

### 시스템 유스케이스(Use Case) 다이어그램(Diagram)

다이어그램은 완료된 초기 제품 기준선과 장기 범위를 함께 보여주는 참고 자료다. 현재 우선순위와 단계는 이 문서의 제품 원칙과 로드맵을 따른다.

![전체 시스템 유스케이스 다이어그램](01_Product.usecase.drawio.svg)

![전체 시스템 유스케이스 상세 다이어그램](new.drawio.svg)

## 현재 범위 밖

다음 항목은 현재 제품화 범위에 포함하지 않는다. `Deferred`는 의도적인 보류이고 `Candidate`는 제품 가치와 범위를 아직 결정하지 않았다는 뜻이다.

- 국내 주식과 미국 주식 데이터 기반
- 뉴스, 공시, 증권사 리포트 수집과 원천 보존
- LLM 요약과 구조화 신호(Signal)
- 호가 원천 스냅샷의 고빈도 장기 보존과 재생(Replay) 분석
- 실시간 체결 히트맵의 추가 확장
- 시장 스캐닝(Scanning), 전략 설계, 백테스트(Backtest)
- 봇 설계, 시뮬레이션, 모의매매, 실거래
- 현재 병목 근거가 없는 다중 워커, 다중 인스턴스, 메시지 큐(Message Queue), 고가용성 고도화

다중 사용자, 권한(Authorization), 과금(Billing), 팀 공유 기능은 장기 후보가 아니라 계속 제외하는 범위다.

## 사용자 시나리오

### 초기 사용자

초기 사용자는 본인의 투자 분석과 자동화 기반을 구축하는 개인 단일 사용자다.

### 현재 제품화 시나리오

1. **데이터 신뢰 확인**
   - 사용자는 관심종목과 코인 상세에서 최신 데이터 시각과 저장된 과거 범위를 확인한다.
   - 데이터가 부족하거나 오래됐으면 화면에서 그 상태를 이해하고 Backfill 또는 운영 상태로 이동한다.

2. **관심 코인 비교와 후보 선택**
   - 사용자는 관심 코인의 현재가, 거래대금, 등락률, 데이터 범위와 분석 기준을 같은 화면에서 비교한다.
   - 정렬과 필터로 후보를 좁히고 더 살펴볼 코인을 선택한다.

3. **코인 상세 분석 진입**
   - 사용자는 선택한 코인의 가격 흐름과 데이터 품질을 확인한다.
   - 어떤 기간과 데이터가 분석에 쓰였는지 이해한 상태에서 다음 판단으로 이동한다.

4. **과거 데이터 준비와 복구**
   - 사용자는 필요한 코인의 Backfill 계획을 만들고 작업 상태를 확인한다.
   - 시스템은 이미 저장된 구간을 다시 요청하지 않고 결측 구간만 수집하며 실패 작업을 안전하게 재개한다.

5. **운영 문제 진단**
   - 운영자는 내부 운영 화면에서 수집 성공률, 최신성, 결측, 워커(Worker), 저장량 변화를 확인한다.
   - 운영 문제 해결은 사용자 가치 화면의 신뢰성을 회복하기 위한 보조 흐름이다.

### 후속 결정 시나리오

- P3 진입 전에는 어떤 분석 지표와 조건이 실제 후보 판단에 쓰이는지 먼저 검증한다.
- 호가 원천 저장은 호가 요약만으로 답할 수 없는 전략 실험이 정의됐을 때 다시 결정한다.
- 주식, 뉴스, LLM, 봇, 거래는 직전 단계의 사용자 가치가 확인된 뒤 별도 시나리오와 위험을 정의한다.

## 수집 품질 정의

수집 품질 지표는 사용자에게 데이터의 신뢰 여부를 설명하고 운영자가 원인을 진단하기 위한 공통 기준이다.

| 지표 | 정의 |
|---|---|
| 성공률(Success Rate) | 관찰 구간의 기대 수집 횟수 대비 성공 횟수 |
| 커버리지(Coverage) | 목표 구간 중 저장된 고유 슬롯(slot)의 비율 |
| 결측률(Missing Rate) | 목표 구간 중 저장되지 않은 슬롯의 비율 |
| 중복률(Duplicate Rate) | 전체 수집 이벤트 중 동일 자연키가 반복된 비율 |
| 최신성 지연(Freshness Lag) | 마지막 성공 데이터 시각부터 현재까지의 지연 |
| 저장 지연(Storage Lag) | 거래소 데이터 시각부터 저장 완료까지의 지연 |
| 저장량 변화(Storage Delta) | 현재 관찰 구간의 저장 행 또는 바이트 증가량 |

기대 횟수는 활성 수집 대상, 데이터 유형, 관찰 구간, 수집 주기, 거래소 달력을 함께 고려한다. 코인은 24시간 시장 기준을 사용하며, 향후 주식은 정규장·확장장·휴장일과 데이터 공급원 제약을 반영한다. 저장량 변화는 수집 정지의 직접 증거가 아니므로 최신성, 성공률, 워커 heartbeat와 함께 판단한다.

### 측정 계약

| 기호 | 의미 |
|---|---|
| `W = [start_at, end_at)` | 품질을 계산하는 관찰 구간(Window) |
| `E(d, i, W)` | 데이터 유형 `d`, 종목 `i`, 구간 `W`에서 수집 계획과 시장 시간 정책상 저장되어야 하는 기대 슬롯(Expected Slot) 수 |
| `S(d, i, W)` | 저장소에 실제 존재하는 고유 슬롯(Unique Slot) 수 |
| `M(d, i, W)` | 결측 구간(Missing Range)으로 판정된 슬롯 수 |
| `A(d, i, W)` | 수집 시도(Collection Attempt) 수 |
| `A_success(d, i, W)` | 성공한 수집 시도 수 |
| `R(d, i, W)` | 저장된 원천 행(Raw Row) 수 |
| `U(d, i, W)` | 저장된 고유 키(Unique Key) 수 |
| `latest_collected_at(d, i)` | 해당 데이터 유형의 최신 수집 시각 |
| `latency_p95(d, i, W)` | 원천 데이터 시각부터 DB 저장 완료까지 걸린 시간의 95퍼센타일(Percentile) |

| 지표 | 산식 |
|---|---|
| 성공률 | `A_success / max(A, 1)` |
| 커버리지 | `S / max(E, 1)` |
| 결측률 | `M / max(E, 1)` |
| 중복률 | `max(R - U, 0) / max(R, 1)` |
| 최신성 지연 | `now_kst - latest_collected_at` |
| 저장 지연 | `latency_p95` |
| 오늘 저장 행 | `count(rows where collected_at >= today_kst_start)` |

품질 점수(Quality Score)는 데이터 유형별 최신성 임계값 `freshness_threshold(d)`와 저장 지연 임계값 `latency_threshold(d)`를 사용한다.

```text
freshness_score = max(0, 1 - freshness_lag / freshness_threshold(d))
latency_score = max(0, 1 - latency_p95 / latency_threshold(d))

quality_score = round(100 * (
  0.35 * coverage
  + 0.25 * freshness_score
  + 0.20 * success_rate
  + 0.10 * (1 - min(duplicate_rate, 1))
  + 0.10 * latency_score
))
```

`quality_score >= 90`이고 필수 지표가 임계값을 넘지 않으면 정상, `70 <= quality_score < 90` 또는 단일 필수 지표 경고가 있으면 주의로 본다. `quality_score < 70`, 최신성 지연이 임계값의 3배 초과, 커버리지 80% 미만, 워커 heartbeat 지연 중 하나라도 있으면 장애로 본다.

| 데이터 유형 | 기대 슬롯 `E(d, i, W)` | 기본 최신성 임계값 |
|---|---|---|
| 현재가 스냅샷(Ticker Snapshot) | 수집 계획상 활성인 1분 버킷(bucket) 수 | 180초 |
| 1분 원천 캔들(Source Candle) | 구간 안의 완성된 1분 캔들 수 | 캔들 종료 후 180초 |
| 호가 요약 | 수집 설정의 샘플링 버킷 수 | 180초 |
| 체결 이벤트(Trade Event) | 기대 슬롯 기반 커버리지에서 제외하고 수신률, 저장 지연, 저장 행 변화로 판정 | 180초 |

## 요구사항

### 상태 정의

| 상태 | 의미 |
|---|---|
| Implemented | 현재 코드와 검증 증적이 있다. |
| Accepted | 제품 방향으로 확정했지만 아직 구현하지 않았다. |
| Candidate | 아이디어는 유효하지만 우선순위와 범위가 아직 미정이다. |
| Deferred | 지금은 의도적으로 미루며 재검토 조건이 있다. |
| Removed | 제품 방향에서 제외한다. |

### 요구사항 목록

| ID | 요구사항 | 제품 단계 | 상태 | 관련 근거 |
|---|---|---|---|---|
| GM-PROD-001 | 업비트 KRW 마켓 24시간 거래대금 상위 100개로 구성된 수집 후보군을 제공하고 최초 설정에서 상위 50개를 활성 수집 대상으로 선택할 수 있다. | 완료 기준선 | Implemented | `docs/Task/M1.md`, `docs/Task/M2.md` |
| GM-PROD-002 | 사용자는 수집 후보군 안에서 최종 활성 수집 대상을 최대 50개로 조정한다. | 완료 기준선 | Implemented | `docs/Task/M2.md` |
| GM-PROD-003 | 활성 대상의 캔들, 현재가, 거래대금, 호가 요약을 수집·저장·조회한다. | 완료 기준선 | Implemented | `docs/Task/M1.md`, `docs/contracts/db/schema.sql` |
| GM-PROD-004 | 호가 요약은 최우선 매수·매도 가격과 수량, 스프레드(Spread), 상위 5호가 또는 10호가 누적 매수·매도 잔량, 호가 불균형(Imbalance), 갱신 시각, 수집 지연을 제공한다. | 완료 기준선 | Implemented | `docs/Task/M1.md`, `docs/contracts/api/openapi.yaml` |
| GM-PROD-005 | 활성 대상과 데이터 유형별 성공률, 커버리지, 결측률, 중복률, 최신성, 저장 지연, 저장량 변화를 확인한다. | 완료 기준선 | Implemented | `docs/Task/M3.md`, 이 문서의 `수집 품질 정의` |
| GM-PROD-006 | 호가 원천 스냅샷은 전략 실험 필요성이 확인될 때 저장 범위, 보존 기간, 재생 분석 필요성을 결정한다. | P3 결정 게이트 | Deferred | `docs/History/2026-07-05-P0-2-제품-의도-재확인과-원칙-정리.md` |
| GM-PROD-007 | 국내 주식의 가격·거래량, 시가총액(Market Cap), 수급, 공매도(Short Selling), 재무지표를 수집하고 품질을 확인한다. | Later | Candidate | 별도 제품 결정 필요 |
| GM-PROD-008 | 미국 주식의 가격·거래량, 시가총액, 재무지표와 지원 가능한 시장 데이터를 수집하고 품질을 확인한다. | Later | Candidate | 별도 제품 결정 필요 |
| GM-PROD-009 | 뉴스, 공시, 증권사 리포트를 수집하고 원천 데이터를 보존한다. | Later | Candidate | 별도 제품 결정 필요 |
| GM-PROD-010 | LLM은 뉴스·공시·리포트를 요약하고 방향, 강도(Strength), 신뢰도(Confidence), 기준 시점, 관련 종목을 구조화 신호로 만든다. | Later | Candidate | 별도 제품 결정 필요 |
| GM-PROD-011 | 가격 데이터와 LLM 신호를 같은 시계열(Time Series)에 정렬해 영향 분석과 전략 입력으로 사용한다. | Later | Candidate | 별도 제품 결정 필요 |
| GM-PROD-012 | 사용자는 수집 데이터를 바탕으로 시장 스캐닝, 관심 대상 심층 분석, 신호 영향 분석을 수행한다. | P2 이후 | Candidate | P2 사용자 시나리오 검증 후 상세화 |
| GM-PROD-013 | 사용자는 시장 데이터와 분석 신호를 조합해 전략을 설계하고 백테스트한다. | P3 이후 | Candidate | P3 결정 게이트에서 상세화 |
| GM-PROD-014 | 사용자는 봇별 투자 금액, 대상 시장, 리스크 한도, 실행 주기, 주문 방식, 전략 가중치(Weight)를 설정한다. | Later | Candidate | 별도 제품 결정 필요 |
| GM-PROD-015 | 봇은 여러 전략을 파이프라인(Pipeline)으로 조합해 매수·매도·보류 판단을 만든다. | Later | Candidate | 별도 제품 결정 필요 |
| GM-PROD-016 | 사용자는 실제 주문 없이 봇 판단, 포지션(Position), 체결 가정, 손익을 시뮬레이션한다. | Later | Candidate | 별도 제품 결정 필요 |
| GM-PROD-017 | 검증된 봇을 모의매매와 실제 자동매매로 확장할 수 있다. | Later | Candidate | 별도 제품 결정 필요 |
| GM-PROD-018 | 제품은 개인 단일 사용자로 운영하며 다중 사용자, 권한, 과금, 팀 공유를 제외한다. | 지속 정책 | Accepted | 이 문서의 `제품 원칙` |
| GM-PROD-019 | 다중 워커·고가용성·메시지 큐·보존 정책은 현재 병목과 확장 필요성이 측정됐을 때 결정한다. | 아키텍처 결정 게이트 | Deferred | `docs/ADR/ADR-0003-M1-단일-워커와-M3-5-다중화-게이트.md` |
| GM-PROD-020 | 외부 알림은 사용자 가치와 운영 필요성이 확인되면 채널, 등급, 빈도 제한, 상태 모델을 결정한다. | Later | Candidate | 별도 제품 결정 필요 |
| GM-PROD-021 | 코인 상세와 전략 작업대의 분석 지표는 P2 사용성 검증 뒤 P3에서 필요한 사용자 정의 범위를 결정한다. P2에서는 고정된 가격·추세·변동성·모멘텀 지표를 분석 화면에 제공한다. | P2 | Accepted | GitHub Issue #12 |
| GM-PROD-022 | 관심종목을 최상단 사용자 화면으로 제공하고 운영 상태와 Backfill을 데이터 수집관리 아래 내부 도구로 제공한다. 프론트엔드(Frontend)는 백엔드가 제공한 화면용 뷰 모델(View Model)을 계산 없이 렌더링하며, 사용자가 정한 관심목록 순서는 모든 관련 화면의 기준 순서다. | 완료 기준선 | Implemented | `docs/Task/M2.md` |
| GM-PROD-023 | Backfill은 후보 선택, 계획 생성 팝업, 시작, `pending` 작업 저장, 워커 실행, 일시정지·재개·중지·삭제 흐름을 제공한다. 일시정지 또는 실패한 작업은 이미 저장된 구간을 제외하고 재개한다. | 완료 기준선 | Implemented | `docs/Task/M2.md`, `docs/Task/M3.md` |
| GM-PROD-024 | Backfill은 저장되지 않은 결측 구간만 요청하고 실패 작업을 안전하게 재개한다. 워커는 가져오기(fetch) 성공 시 heartbeat를 갱신하고 DB 배치(batch) upsert 완료 뒤 `rows_written`과 `last_completed_at`을 갱신한다. 기본 저장 배치는 최대 3000개이며 외부 설정으로 바꿀 수 있고 로그 수준도 운영 설정으로 조정할 수 있다. | 완료 기준선 | Implemented | `docs/ADR/ADR-0006-M1-백필-배치-저장과-heartbeat-갱신.md` |
| GM-PROD-025 | 사용자는 수집 후보군에서 선택한 관심 코인만 분석 화면에서 연다. 관심목록 밖 코인의 분석 구독은 허용하지 않는다. | P1·P2 | Accepted | GitHub Issue #12 |
| GM-PROD-026 | 사용자는 관심 코인 하나의 최대 3년 차트에서 월·주·일·시·30분·10분·5분·1분 봉, 거래량, SMA, EMA, 볼린저 밴드(Bollinger Bands), RSI를 확인한다. 1차 진입 화면은 1년 일봉이다. | P2 | Accepted | GitHub Issue #12 |
| GM-PROD-027 | 분석 화면은 WebSocket으로 세션, 차트, 지표, 현재가·호가 요약, 체결 요약을 독립 메시지로 수신하고, 재연결하면 선택 상태를 다시 구독한다. | P2 | Accepted | `docs/contracts/api/realtime-analysis-websocket.schema.json` |
| GM-PROD-028 | 시스템은 1분 원천봉을 보존하고 5·10·30분·시·일·주·월 봉을 집계 테이블(Materialized Rollup)에 증분 생성한다. 분석 조회는 최신 집계를 우선 사용하고, 집계가 뒤처진 경우 원천봉 파생으로 정확성을 보존한다. | P2.1 | Accepted | GitHub Issue #13, ADR-0009 |
| GM-PROD-029 | 운영자는 시스템 관리 화면에서 Backfill·실시간 수집·집계 워커의 상태, 실행 중인 코인과 데이터 유형, 집계 작업의 전체·완료 대상 수와 진행률을 실시간으로 확인한다. 집계가 오래되면 시스템은 자동으로 집계 작업을 시작한다. | P2.1 | Accepted | GitHub Issue #13, `docs/contracts/db/schema.sql` |
| GM-PROD-030 | 업비트 API 테스트 메뉴는 시세 조회, 거래·자산 관리, WebSocket의 2레벨 메뉴와 기능 그룹 탭을 제공하고, 공식 문서에 수록된 모든 현재 REST·WebSocket 기능을 선택할 수 있다. | P2.2 | Implemented | GitHub Issue #19~#24, `docs/contracts/upbit/upbit-api-catalog.yaml` |
| GM-PROD-031 | 공통 페어·자산 선택과 기능별 필수·선택 파라미터를 좌측 요청 패널에서 설정하고, 우측 결과 패널은 차트·목록·호가창 등 응답에 적합한 시각화와 원본 요청·응답 추적 정보를 제공한다. | P2.2 | Implemented | GitHub Issue #21~#24, `docs/Test/2026-07-16-업비트-전체-API-작업대-통합-검증.md` |
| GM-PROD-032 | 브라우저는 키를 보유하거나 업비트에 직접 연결하지 않고 별도 업비트 API 게이트웨이를 사용한다. 게이트웨이는 공식 그룹별 요청 제한과 인증을 적용하고 키·JWT를 응답·로그에 노출하지 않는다. | P2.2 | Implemented | ADR-0011, GitHub Issue #19·#20·#23·#24 |
| GM-PROD-033 | 실제 주문 생성·취소, 자산 이전, 입출금, 트래블룰 검증은 실행하지 않는다. 주문 검증은 공식 주문 생성 테스트 API만 사용하고, 그 밖의 상태 변경 API는 요청 구성 확인과 로컬 차단 오류까지만 제공한다. | 지속 안전 정책 | Implemented | ADR-0011, `docs/Test/2026-07-16-업비트-전체-API-작업대-통합-검증.md` |

## 정책

- 시스템 프로세스와 코인 수집은 업비트 시장 특성에 맞춰 24시간 365일 운영한다.
- 향후 주식 수집은 거래소 운영 시간, 휴장일, 데이터 공급원 제약을 반영하며 비수집 시간은 장애로 취급하지 않는다.
- 원천 데이터 보존은 재처리와 감사(Audit) 가치, 비용, 실제 분석 필요성을 함께 고려한다.
- 호가 원천 데이터가 없는 과거 구간에서는 호가 기반 전략을 완전하게 검증할 수 없음을 명시한다.
- LLM 분석 결과는 투자 판단의 보조 입력이며 사실 데이터와 같은 확정 정보로 취급하지 않는다.
- 제품은 투자 수익을 보장하지 않으며 매수·매도 추천의 법적 책임을 제품 목표로 두지 않는다.

## 로드맵

| 단계 | 목표 | 완료 기준 | 세부 계획 수준 |
|---|---|---|---|
| P0 | 현재 산출물 정리와 제품 기준 재정렬 | 운영 도구와 사용자 가치 화면의 경계, 요구사항 상태, 가까운 제품 단계가 문서화되고 아키텍처·계약과 정렬된다. | 구체 |
| P1 | 코인 전용 관심목록과 신뢰 가능한 데이터 기반 | 사용자가 전체 코인 목록에서 관심 코인을 선택하고 데이터 범위, 최신성, 결측 상태와 복구 경로를 이해한다. | 구체 |
| P2 | 실시간 코인 분석 작업대 | 사용자가 관심 코인 하나의 차트·거래량·기술 지표·현재가·호가·체결 흐름을 읽고 시간 단위를 전환한다. | 구체 |
| P2.1 | 집계 기반 분석 데이터 운영 | 시스템이 집계 테이블을 최신화하고 운영자가 수집·Backfill·집계 작업의 대상과 진행률을 확인한다. | 구체 |
| P2.2 | 업비트 전체 API 테스트 작업대 | 개발자가 전체 REST·WebSocket 계약과 인증·요청 제한·응답 형태를 실제 자산 변경 없이 검증한다. | 구체 |
| P3 | 전략 실험 준비 | 지표와 조건을 조합하기 전에 필요한 데이터 모델, 화면, 실험 단위, 호가 원천 필요성을 결정한다. | 결정 게이트 |
| Later | 주식, 뉴스, LLM, 봇, 모의매매, 실거래 | 각 영역 진입 전에 사용자 가치, 데이터, 비용, 리스크를 별도 제품 질문과 ADR로 승인한다. | 방향만 |

## 변경 규칙

- 제품 범위, 요구사항, 정책, 로드맵이 바뀌면 이 문서를 먼저 갱신한다.
- 제품 단계는 사용자 결과와 완료 기준을 먼저 정의한 뒤 GitHub Issue로 실행 단위를 만든다.
- 아키텍처, API·DB·메시지 계약, 되돌리기 어려운 결정은 해당 단일 기준 문서와 ADR에서 관리하고 이 문서에는 제품 의미만 남긴다.
- 브레인스토밍 또는 외부 스킬이 만든 장기 산출물은 별도 명세 디렉터리에 두지 않고 GitHub Issue, `docs/History/`, `docs/ADR/` 중 맞는 위치로 흡수한다.
- 구현 로그와 검증 결과는 GitHub Issue, PR(Pull Request), `docs/Test/`, `docs/History/`에 기록한다.
