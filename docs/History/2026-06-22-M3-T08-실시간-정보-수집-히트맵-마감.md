# 2026-06-22 M3-T08 실시간 정보 수집 히트맵 마감

Date: 2026-06-22
Related Task: `docs/Task/M3.md`
Related Test: `docs/Test/2026-06-22-M3-T08-실시간-정보-수집-히트맵-마감-검증.md`

## 변경 요약

- M3-T08 Task 문서가 `Status: Todo`와 미완료 체크박스를 유지하던 드리프트(drift)를 정리했다.
- 현재 코드 기준으로 `realtimeCollectionHeatmap` 모델, 저장소 집계, OpenAPI 계약, FastAPI 응답 변환, React 렌더링, E2E 기대값이 반영되어 있음을 확인했다.
- Task 상태를 `Done`으로 갱신하고, 실행 결과와 마감 검증, 추적성 섹션을 추가했다.
- M3-T08 전용 검증 증적을 `docs/Test/`에 추가했다.

## 확인한 구현

- 활성 수집 대상 최대 50개 기준 최근 24시간 실시간 수집 히트맵을 계산한다.
- 데이터 유형은 `source_candle`, `ticker_snapshot`, `orderbook_summary`를 사용한다.
- 각 시간 버킷은 기대 row 수, 실제 row 수, 타입별 row 수, 수집 비율, 상태(`none`, `low`, `collecting`, `high`)를 제공한다.
- 프론트엔드는 API가 제공한 View Model을 렌더링하고, 임의 합성 지표를 만들지 않는다.
- 기존 `collectionActivity`는 7일 x 24시간 보조 지표로 유지한다.

## 검증

- `uv run pytest tests/contracts/test_api_contract.py tests/api/test_operations_api.py tests/shared/test_repository_behavior.py -q`: PASS, `33 passed, 1 warning`
- `npm --workspace apps/web run test -- src/operationsDisplay.test.ts src/api.test.ts`: PASS, `2 files, 11 tests passed`

## 리스크

- 이번 마감 감사에서는 전체 E2E를 새로 실행하지 않았다. 현재 로컬 앱/인프라 프로세스가 stopped 상태였고, E2E 기대값과 기존 Chromium E2E 통과 증적으로 보완했다.
- M3 전체 완료 여부는 이 Task와 별개다. 제품 로드맵 기준 M3에는 호가 원천 스냅샷(Source Orderbook Snapshot) 저장 시작이 남아 있다.

## 후속 작업

- M3-T19로 호가 원천 스냅샷(Source Orderbook Snapshot) 저장 계약과 구현 Task를 생성한다.
