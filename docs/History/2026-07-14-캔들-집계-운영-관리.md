# 2026-07-14 캔들 집계 테이블과 시스템 관리

## 변경 요약

- 1분 원천봉과 일봉을 `5m/10m/30m/60m/1d/1w/1M` 분석용 집계 테이블로 멱등 저장하는 모델, DB 계약, 작업·대상 진행률을 추가했다.
- 최신 시각이 원천보다 오래된 활성 코인·주기를 자동 감지해 집계 작업을 만들고 별도 캔들 집계 워커가 처리한다.
- `시스템 관리` 메뉴와 `/v1/realtime/system-management` WebSocket을 추가해 실시간 수집·Backfill·집계의 코인별 대상, 데이터 유형, 상태, 진행률을 작은 상태 메시지로 갱신한다.
- 로컬·배포 프로필에 `candle-aggregation-worker`를 추가했다.
- 2026-07-16 완료 기준 재검토에서 시스템 관리 WebSocket과 화면에 집계 워커의 실제 heartbeat 상태·최신 시각을 추가하고, 집계 작업의 대기 대상 수를 전체·완료·실행·실패 수와 함께 표시하도록 보완했다.
- 후속 리뷰에서 단일 집계 대상이 30초 넘게 걸리면 워커가 실제 동작 중이어도 `stale`로 표시될 수 있음을 확인했다. 1차로 진행 콜백(progress callback)을 연결했지만, 재리뷰에서 첫 콜백 전 장기 구간과 처리량 비례 DB 쓰기가 남음을 확인해 작업 수명 독립 5초 ticker로 교체했다.
- 하트비트 실행기(heartbeat ticker)는 집계와 별도 저장소 연결을 사용하고 DB I/O(Input/Output)를 2초로 제한한다. 중지 이벤트(stop event) 뒤 최대 3초만 스레드 합류(thread join)하며, 초과 시 오류 로그를 남기고 데몬 스레드(daemon thread)가 워커 종료를 막지 않게 한다.
- SQLite 집계는 전체 쓰기를 단일 트랜잭션(transaction)으로 묶어 하트비트 교차 실행이나 후속 실패가 부분 집계를 커밋(commit)하지 않게 했다. 저장소·집계 계산의 행 수 기반 하트비트 콜백을 제거해 3년 1분봉 같은 대량 대상의 PostgreSQL 연결·UPSERT 증폭을 없앴다.
- 격리 E2E 시드도 실제 워커 생존 주기를 모사해 31초 뒤에도 `동작 중`을 유지하도록 했고, 전체 350건·대기 350건과 상태별 합계 불변식을 정확히 검증한다.

## 결정과 리스크

- 결정 근거는 [ADR-0009](../ADR/ADR-0009-캔들-집계-테이블과-자동-워커.md), 계약은 [시스템 관리 WebSocket](../contracts/api/realtime-system-management-websocket.md)을 따른다.
- DB 스키마 변경은 없다. 기존 `collection_worker_heartbeats`의 `candle_aggregation` 행과 집계 대상 상태를 API 뷰 모델에 노출하는 변경이다.
- Docker의 PostgreSQL 17.10 컨테이너에 단일 기준 마이그레이션을 적용한 뒤 혼합 상태(`완료 1·실행 1·대기 4·실패 1`)의 집계 건수와 진행률을 실제 저장소로 검증했다. 같은 시나리오를 SQLite에도 적용해 구현 동등성을 확인했다.
- SQLite는 운영 런타임이 아닌 격리 테스트 어댑터다. 파일 DB는 별도 하트비트 연결을 사용하고, 독립 연결로 같은 DB를 공유할 수 없는 `:memory:` 테스트만 같은 저장소와 전체 집계 잠금을 사용한다.

## 검증

[검증 증적](../Test/2026-07-14-캔들-집계-운영-관리-검증.md)을 따른다.
