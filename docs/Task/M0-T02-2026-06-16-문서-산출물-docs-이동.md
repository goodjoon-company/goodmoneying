# M0-T02-2026-06-16-문서-산출물-docs-이동

Status: Done
Created: 2026-06-16
Updated: 2026-06-16
Owner: Codex

## 목표

문서 산출물과 문서 디렉터리를 `docs/` 아래로 이동한다.

## 요구사항 링크

- User request: `docs/` 밑으로 산출물 문서와 폴더 이동

## 범위

- 포함: Product, Architecture, contracts, ADR, Task, Test, History, 문서 지도 이동
- 포함: root `README.md`를 `docs/` 문서 지도 포인터로 재작성
- 제외: `AGENTS.md` 이동

## 현재 맥락

초기 문서 구조는 root에 생성되어 있었다. 문서 산출물을 한 곳에 모으기 위해 `docs/` 하위 구조로 재배치한다.

## 설계 메모

- `AGENTS.md`는 에이전트가 root에서 발견해야 하는 repo-local 규칙이므로 root에 유지한다.
- 산출물 문서의 기준 경로는 `docs/`로 통일한다.
- root `README.md`는 프로젝트 진입점 역할만 하고 상세 문서 지도는 `docs/README.md`에 둔다.

## 계약 링크

- `docs/contracts/README.md`

## 완료 기준

- 산출물 문서와 문서 디렉터리가 `docs/` 아래에 있다.
- `AGENTS.md`, root `README.md`, 문서 지도와 주요 규칙 문서가 새 경로를 가리킨다.
- 이동 작업의 검증 기록과 인계 기록이 남아 있다.

## 검증

- `rg --files`: 문서 파일이 `docs/` 아래로 이동됐는지 확인
- `rg -n '01_Product|02_Architecture|contracts|ADR|Task|Test|History|README\\.md' AGENTS.md docs`: 잔여 경로 표현 확인
- `git status --short`: 이동 후 신규 파일 상태 확인

## 추적성

- Related Test: `docs/Test/2026-06-16-문서-docs-이동-검증.md`
- Related History: `docs/History/2026-06-16-문서-산출물-docs-이동.md`
