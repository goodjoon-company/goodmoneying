# P2 요약: 연구 데이터 계층과 Data Lab

GitHub Issue: [#29](https://github.com/goodjoon-company/goodmoneying/issues/29)

## 현재 완료 결과

- P2-1은 캔들 개정 원장과 집계 계보를 추가해 원천 수정과 파생 결과의 입력 프런티어(frontier)를 추적한다.
- P2-2는 원천·품질 변경을 내구성 무효화 큐로 전파하고 불변 증분 집계를 같은 트랜잭션 경계에서 갱신한다.
- P2-3은 버전 지표와 시장 통계를 추가 전용 물질화(materialization)로 저장하고 안정 cursor REST 조회를 제공한다.
- P2-4는 1분 미시구조 통계를 체결·호가 receipt와 연결하고 원천 품질과 계산 상태를 분리한다.
- P2-5는 dataset build 수명주기와 게시 후 불변 dataset version, coverage, exact member REST 계약을 구현한다.
- P2-6은 Data Lab 화면에서 build 생성·복제, build 목록 REST polling, version 목록, coverage heatmap, series 선택, exact member 표·차트, A/B 비교를 제공한다.
- P2-7은 `/v1/realtime/analysis/stream`에 내부 WebSocket envelope를 적용하고 `/v1/realtime/analysis` legacy alias를 유지해 topic·scope별 sequence, 서명 cursor, heartbeat, `snapshot_required`, 클라이언트 gap 적용 중단을 제공한다.
- P2-8은 `/v1/realtime/analysis/snapshot` REST 복구 snapshot, cursor 만료 후 자동 재동기화, 유효 REST cursor 재구독의 초기 snapshot 재전송 생략, `slow_consumer` backpressure 신호를 제공한다.

## 현재 제외·대기

- 주식시장 기능은 현재 목표의 비범위다.
- Data Lab은 브라우저에 운영 토큰을 보관하지 않고 동일 출처 프록시가 주입하는 경계를 유지한다.
- build, version, exact member의 다음 page는 cursor 기반 더 보기로 명시 탐색한다.
- 분석 WebSocket은 P2 envelope를 우선 계약으로 사용하되 전환기 호환을 위해 legacy top-level `version/type/sentAt`을 유지한다.

## 검증과 후속

P2-7의 실제 명령과 증적은 [`P2-7 내부 스트림 검증`](../Test/2026-07-18-P2-7-내부-스트림-검증.md)에 둔다. P2-8의 실제 명령과 증적은 [`P2-8 snapshot 복구 검증`](../Test/2026-07-18-P2-8-snapshot-복구-검증.md)에 둔다.

P2는 2026-07-18 기준 완료됐다. 최종 구현 SHA는 `daab497667a6000f7dc8efb615003903084c3043`이고 원격 CI [`29635495140`](https://github.com/goodjoon-company/goodmoneying/actions/runs/29635495140)이 성공했다. 문서 완료 상태만 반영하는 후속 커밋의 CI까지 확인한 뒤 Issue #29를 닫는다.
