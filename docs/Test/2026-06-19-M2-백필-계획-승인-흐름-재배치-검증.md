# 2026-06-19 M2 백필 계획 승인 흐름 재배치 검증

Status: Done
Related Task: `docs/Task/M2.md`

## 검증 대상

- 수집 대상/설정 화면 기반 백필 계획 생성 레이어 팝업(layer popup)
- 백필 승인 패널의 계획 목록과 승인 버튼 상태
- 워커(Worker)의 기존 캔들(Source Candle) 구간 중복 요청 회피
- 기존 API/API 클라이언트와 React 빌드 회귀

## RED 확인

```bash
npm --workspace apps/web run test -- App.test.tsx
uv run pytest tests/worker/test_collector_behavior.py -k "first_missing or runs_approved_backfill"
```

결과:

```text
App.test.tsx: 백필 계획 생성 버튼과 레이어 팝업이 없어 실패
test_worker_starts_backfill_from_first_missing_candle_after_existing_start: 워커가 2026-01-01 00:00부터 요청해 실패
```

## 자동화 검증

```bash
npm --workspace apps/web run test -- App.test.tsx api.test.ts
npm --workspace apps/web run build
uv run pytest tests/worker/test_collector_behavior.py tests/shared/test_repository_behavior.py tests/api/test_operations_api.py
uv run pytest -q
npm test
npm run build
uv run ruff check .
uv run mypy apps packages tests
git diff --check
```

결과:

```text
Vitest 2 files, 7 tests passed
Vite build passed
pytest 25 passed, 1 warning
pytest 전체 69 passed, 1 warning
npm test 7 passed
npm run build passed
ruff passed
mypy passed
git diff --check passed
```

## E2E 검증

```bash
npm run e2e
```

결과:

```text
Playwright chromium 1 passed
```

## 미검증 또는 리스크

- 실제 업비트 라이브 백필은 실행하지 않았다.
- 승인 전 계획 목록은 브라우저 세션 로컬 상태이므로 새로고침 후 복구하지 않는다.
