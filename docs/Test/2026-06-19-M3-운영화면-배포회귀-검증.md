# 2026-06-19 M3 운영화면 배포회귀 검증

## 대상

- Task: `docs/Task/M3-T02-2026-06-19-001-운영화면-배포회귀-수정.md`
- 범위: 운영 화면 401 회귀, 첫 화면 로딩 경량화, 운영 상태/수집 대상 표시 개선

## 사전 재현

| 명령 | 결과 |
|---|---|
| `curl -sS -o /tmp/gm_put -w '%{http_code} %{time_total}\n' -X PUT http://100.115.38.59:8000/v1/collection-targets -H 'Content-Type: application/json' -H 'X-Operator-Token: local-dev-token' --data '{"instrumentIds":[],"reason":"repro"}'` | `401`, `{"code":"UNAUTHORIZED","message":"운영 토큰이 없거나 올바르지 않습니다."}` |
| `curl -fsS http://100.68.208.102:8080/` | 운영 web HTML 응답, `/assets/index-DVZRVVAo.js` 사용 확인 |

## 자동 검증

| 명령 | 결과 |
|---|---|
| `uv run ruff check .` | PASS, `All checks passed!` |
| `uv run mypy apps/api apps/worker packages/shared tests` | PASS, `Success: no issues found in 27 source files` |
| `uv run pytest` | PASS, `68 passed, 1 warning` |
| `npm test` | PASS, `2 passed`, `6 passed` |
| `npm --workspace apps/web run build` | PASS, Vite production build 성공 |
| `npm run e2e` | PASS, Chromium E2E 1개 통과 |

## 확인한 동작

- 운영 쓰기 요청은 브라우저 번들 토큰이 아니라 Nginx 프록시(proxy)가 `GOODMONEYING_OPERATOR_TOKEN`을 `X-Operator-Token`으로 붙이는 구조가 됐다.
- 첫 운영 스냅샷은 `/api/v1/dashboard/summary`, `/api/v1/backfill/jobs`만 호출한다.
- 후보/시장/상세/캔들 데이터는 수집 대상 탭, 시장 리스트 탭, 상세 레이어 진입 시 조회한다.
- 거래대금 표시값은 `₩100,000,000,000` 형식으로 통화기호와 천 단위 구분자를 포함한다.
- 수집 범위 표시값은 `2026-01-01 00:00 KST ~ NOW` 형식이다.
- 품질 배지는 hover tooltip으로 품질 상세 근거를 제공한다.
- 운영 상태 화면은 구간형 그래프 대신 실시간 수집, Backfill, 가격 분봉 저장 수 테이블을 보여준다.

## 미검증 또는 후속

- 운영 서버에 새 이미지를 배포한 뒤 `web.env`에 `GOODMONEYING_API_INTERNAL_URL`과 `GOODMONEYING_OPERATOR_TOKEN`이 들어있는지 확인해야 한다.
- PostgreSQL materialized view 또는 수집 시점 통계 테이블은 이번 수정 범위에서 제외했다.
