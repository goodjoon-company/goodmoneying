# 2026-06-20 M3-T06 프론트엔드 모듈화 검증

Related Task: `docs/Task/M3.md`

## 목적

운영 화면 프론트엔드(Frontend) Module 분리 뒤 기존 사용자 흐름, TypeScript build, 브라우저 E2E(End-to-End)가 유지되는지 검증한다.

## 환경

- 브랜치: `codex/design_mod_v0.1`
- 위치: `/Users/goodjoon/project/goodjoon/goodmoneying`
- 실행일: 2026-06-20

## 기준선 검증

| 명령 | 결과 | 메모 |
|---|---|---|
| `npm --workspace apps/web run test` | Pass | 3 files, 12 tests 통과 |
| `npm run build` | Pass | `tsc -b && vite build` 통과 |

## RED 확인

| 대상 | 명령 | 기대 실패 |
|---|---|---|
| 운영 데이터 접근 Module | `npm --workspace apps/web run test -- api.test.ts` | `./operationsData` Module 부재 |
| 수집 대상/백필 workflow Module | `npm --workspace apps/web run test -- targetBackfillWorkflow.test.ts` | `./targetBackfillWorkflow` Module 부재 |
| 운영 표시 모델 Module | `npm --workspace apps/web run test -- operationsDisplay.test.ts` | `./operationsDisplay` Module 부재 |
| 운영 화면 상태 Module | `npm --workspace apps/web run test -- useOperationsConsole.test.tsx` | `./useOperationsConsole` Module 부재 |
| 후보 정렬 접근성 Interface | `npm --workspace apps/web run test -- App.test.tsx` | `combobox` name `후보 정렬` 부재 |

## 최종 자동화 검증

| 명령 | 결과 | 증거 |
|---|---|---|
| `npm --workspace apps/web run test` | Pass | 6 files, 23 tests 통과 |
| `npm run build` | Pass | `tsc -b && vite build`, Vite production build 통과 |
| `npm run e2e` | Pass | Chromium 1 test 통과 |

## 구조 검증

| 항목 | 결과 |
|---|---|
| `apps/web/src/api.ts` | 840줄에서 451줄로 축소, fixture Implementation 제거 |
| `apps/web/src/App.tsx` | 표시 helper, workflow, 상태 전이 분리 |
| `apps/web/src/operationsData.ts` | `OperationsDataClient` Interface와 HTTP Adapter 추가 |
| `apps/web/src/operationsFixture.ts` | fixture Adapter와 `demoSnapshot()` Implementation 이동 |
| `apps/web/src/targetBackfillWorkflow.ts` | 후보 검색/정렬, 50개 제한, 백필 draft 상태 전이 추가 |
| `apps/web/src/operationsDisplay.ts` | KST 표시, 퍼센트, 저장량, fallback 표시 helper 추가 |
| `apps/web/src/useOperationsConsole.ts` | 스냅샷, 상세 레이어, 선택 거래 상품 상태 전이 추가 |

## 코드 리뷰

- 제품 정합성: `docs/01_Product.md`의 데이터 수집관리 화면과 백필 계획 승인 흐름을 유지한다.
- 아키텍처 정합성: `docs/02_Architecture.md`의 React Query + HTTP 폴링(Polling) 결정은 변경하지 않았다.
- 계약 정합성: `docs/contracts/api/openapi.yaml` 필드 의미와 요청/응답 계약은 변경하지 않았다.
- 테스트 정합성: 신규 Module은 unit test를 먼저 추가한 뒤 구현했고, 최종 unit/build/E2E가 통과했다.
- 남은 리스크: OpenAPI 타입 자동 생성은 아직 도입하지 않았다. 현재는 수동 타입을 유지하되 fixture와 HTTP Adapter Seam을 분리했다.
