# 2026-06-21 worker 현황판 계약과 화면 검증

Status: Done
Created: 2026-06-21
Updated: 2026-06-21
Related Task: `docs/Task/M3.md`

## 검증 항목

- API 계약(OpenAPI)이 `DashboardSummary.workerStatus`를 포함한다.
- DB 계약이 `collection_worker_heartbeats`를 포함한다.
- API 요약 응답이 realtime/backfill worker 상태와 오류 지표를 반환한다.
- 운영 상태 첫 카드가 worker 현황판으로 표시된다.
- 오류 수 클릭 시 상세 레이어 팝업이 표시된다.
- 자동화된 E2E(End-to-End)가 통과한다.

## 실행 결과

- `uv run pytest tests/contracts/test_api_contract.py tests/api/test_operations_api.py tests/worker/test_worker_cli.py -q`
  - 결과: `27 passed, 1 warning`
  - 확인: OpenAPI 계약, API workerStatus 응답, worker heartbeat 기록 테스트 통과
- `uv run mypy apps/api apps/worker packages/shared tests`
  - 결과: `Success: no issues found in 30 source files`
- `uv run pytest -q`
  - 결과: `89 passed, 1 warning`
- `npm test`
  - 결과: `7 passed`, `28 tests passed`
- `npm run build`
  - 결과: TypeScript 빌드와 Vite production build 통과
- `npm run e2e`
  - 결과: Chromium Playwright `1 passed`
- `curl -fsS http://127.0.0.1:8000/v1/dashboard/summary`
  - 결과: `workerStatus.realtime.status=running`, `workerStatus.backfill.status=stale` 확인
  - 비고: 현재 로컬 런타임에서 backfill worker는 중지 상태이므로 `stale` 표시가 정상이다.

## 미검증 항목

- 없음.
