# 2026-06-21 백필 시작 흐름과 멈춤 버튼 검증

Related Task: `docs/Task/M3-T14-2026-06-21-001-백필-시작-흐름과-멈춤-버튼.md`
Date: 2026-06-21

## 범위

- 백필 계획 생성 팝업의 `백필 시작` 버튼
- `POST /v1/backfill/jobs` 기반 `pending` 작업 생성
- 백필 계획 승인 버튼 제거
- 백필 작업 패널의 멈춤(Pause), 중지(Stop), 삭제(Delete) 제어
- API 계약(OpenAPI)과 화면 테스트 정합성

## 증적

| 명령 | 결과 | 비고 |
|---|---|---|
| `uv run pytest tests/api/test_operations_api.py::test_backfill_job_start_and_control tests/contracts/test_api_contract.py -q` | Pass | 8 passed, StarletteDeprecationWarning 1건 |
| `npm --workspace apps/web run test -- src/App.test.tsx` | Pass | 9 passed |
| `uv run ruff check .` | Pass | All checks passed |
| `uv run mypy apps/api apps/worker packages/shared tests` | Pass | Success, 31 source files |
| `uv run pytest -q` | Pass | 94 passed, StarletteDeprecationWarning 1건 |
| `npm test` | Pass | 7 files, 29 tests passed |
| `npm run build` | Pass | TypeScript build와 Vite production build 통과 |
| `npm run e2e` | Pass | Chromium 1 scenario passed |
| `git diff --check` | Pass | 공백 오류 없음 |

## 판정

통과. 백필 계획 생성 팝업의 `백필 시작` 버튼, 직접 `pending` 작업 생성, 승인 버튼 제거, 멈춤(Pause) 제어가 API, 화면, E2E 테스트에서 확인됐다.
