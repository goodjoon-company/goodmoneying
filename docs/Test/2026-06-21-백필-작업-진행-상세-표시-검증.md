# 2026-06-21 백필 작업 진행 상세 표시 검증

Date: 2026-06-21
Related Task: `docs/Task/M3-T16-2026-06-21-001-백필-작업-진행-상세-표시.md`
Environment: macOS local, Python 3.14 가상환경(Virtual Environment), Node.js workspace, Playwright Chromium

## 검증 대상

- `/v1/backfill/jobs` 작업별 진행 상세 응답
- Backfill 관리 화면의 진행 상세 카드 표시
- Backfill 관리 화면의 `결측 구간 처리` 레이블과 hover 정보 아이콘
- 백필 작업 전체 진행률의 현재 대상 결측 구간 처리 비율 반영
- 운영 상태 코인별 수집 상태의 전체 대상 표시와 헤더 정렬
- 중지된 백필 작업 최근 30일 필터
- Backfill 관리 10초 주기 갱신
- 백필 워커(Backfill Worker)의 결측 구간 처리 수 기록

## 실행 명령

| 명령 | 결과 | 메모 |
|---|---|---|
| `.venv/bin/python -m pytest tests/api/test_operations_api.py::test_backfill_jobs_expose_live_progress_and_hide_old_stopped_jobs -q` | 통과 | API 응답 상세 필드와 오래된 stopped 작업 필터 검증 |
| `npm --workspace apps/web run test -- src/App.test.tsx -t "코인별 수집 상태는 전체 대상\|백필 작업 목록"` | 통과 | 코인별 수집 상태 전체 50개 표시, 24H 거래대금 헤더 정렬, 백필 카드 레이블/정보 아이콘 검증 |
| `npm --workspace apps/web run test -- src/App.test.tsx -t "수집 대상 화면에서 백필 작업 목록과 실행 상태를 표시한다"` | 통과 | 작업 카드 진행 상세 표시 검증 |
| `.venv/bin/python -m pytest tests/shared/test_repository_behavior.py tests/worker/test_collector_behavior.py tests/api/test_operations_api.py tests/contracts/test_api_contract.py tests/contracts/test_db_contract.py -q` | 통과 | 51 passed, 1 warning |
| `npm --workspace apps/web run test -- src/App.test.tsx src/api.test.ts src/useOperationsConsole.test.tsx src/targetBackfillWorkflow.test.ts` | 통과 | 21 passed |
| `.venv/bin/python -m pytest tests/shared/test_postgres_repository_schema.py tests/contracts/test_timezone_contract.py tests/scripts/test_dev_script.py tests/worker/test_worker_cli.py -q` | 통과 | 18 passed |
| `npm test -- --run` | 통과 | 30 passed, npm `--run` 경고 있음 |
| `.venv/bin/python -m pytest -q` | 통과 | 97 passed, 1 warning |
| `npm run build` | 통과 | TypeScript 빌드와 Vite 빌드 통과 |
| `npm run e2e` | 통과 | Playwright 1 passed, 코인별 수집 상태 50개 표시/24H 거래대금 정렬 전환/백필 정보 아이콘 포함 |
| `./dev.sh app restart api`, `./dev.sh app restart web`, `./dev.sh app restart backfill-collection-worker` | 통과 | 로컬 실행 프로세스 새 코드 반영 |

## 수동 검증

- `./dev.sh app status all`로 API, web, realtime worker, backfill worker 실행 상태를 확인했다.
- `http://127.0.0.1:8000/v1/backfill/jobs`를 Python `urllib`로 조회했다.
- 실제 응답에서 작업 12가 `totalTargetCount=50`, `completedTargetCount=5`, `runningTargetIndex=6`, `currentTarget=KRW-WLD`, `processedMissingRangeCount=44`, `estimatedMissingRangeCount=7807`, `estimatedRequestCount=11200`을 반환함을 확인했다.

## 미검증 항목

- 실제 브라우저에서 장시간 관찰하며 10초마다 숫자가 계속 증가하는지는 수동 장기 관찰하지 않았다.
- 백필 결측 구간 병합 최적화는 이번 범위가 아니다.

## 결론

백필 작업 목록 진행 상세 표시, 10초 갱신 경로, 최근 30일 stopped 필터, 워커 진행 기록이 자동화 테스트와 실제 로컬 API 응답으로 검증됐다.
