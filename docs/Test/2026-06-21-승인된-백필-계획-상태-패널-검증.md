# 2026-06-21 승인된 백필 계획 상태 패널 검증

## 상태

Done

## 검증 대상

- 수집 대상/설정 화면의 승인된 백필 계획 목록
- 백필 job 실행 상태, 데이터 타입, 생성 시각, 진행률 표시
- 기존 승인 대기 draft 계획 목록과 승인된 job 목록의 분리

## 실행 명령

```bash
npm --workspace apps/web run test -- src/App.test.tsx
```

결과: `1 passed`, `9 passed`

```bash
npm test
```

결과: `7 passed`, `30 passed`

```bash
npm run build
```

결과: 성공

```bash
npm run e2e
```

결과: Chromium `1 passed`

## 확인 내용

- 수집 대상/설정 화면에 `승인된 백필 계획` heading이 표시된다.
- 승인된 job 목록에는 `작업 77`, `실행 중`, `42.5%`, `1분 캔들(Source Candle)`, 생성 시각이 표시된다.
- 완료된 job은 `완료` 상태로 표시된다.
- 승인 전 draft 목록은 `백필 계획 목록`에 유지되고, 승인된 job 목록은 `승인된 백필 계획 목록`으로 분리된다.

## 남은 검증

- 실제 운영 데이터에서 여러 백필 job이 쌓인 상태의 스크롤 밀도와 긴 상태 문자열 표시를 배포 후 한 번 더 확인한다.
