# 2026-06-22 M3-T08 실시간 정보 수집 히트맵 마감 검증

Date: 2026-06-22
Related Task: `docs/Task/M3-T08-2026-06-20-002-실시간-정보-수집-현황-히트맵-개선.md`
Environment: macOS, zsh, Python 3.14, uv, Node.js 24, npm, Vitest

## 검증 대상

- `DashboardSummary.realtimeCollectionHeatmap` API 계약(Contract)
- SQLite/PostgreSQL 저장소의 활성 수집 대상 최대 50개, 최근 24시간 히트맵 계산
- FastAPI 응답 DTO(Data Transfer Object) 매핑
- React 화면의 `실시간 정보 수집 현황` 카드 렌더링
- 구버전 응답 fallback과 E2E(End-to-End) 표시 기대값

## 코드 확인

| 확인 위치 | 결과 |
|---|---|
| `packages/shared/goodmoneying_shared/models.py` | `RealtimeCollectionHeatmapBucket`, `RealtimeCollectionHeatmapRow`, `DashboardSummary.realtime_collection_heatmap` 존재 |
| `packages/shared/goodmoneying_shared/postgres_repository.py` | `_realtime_collection_heatmap()`이 활성 대상 최대 50개, 최근 24시간, 타입별 실제/기대 row 수를 계산 |
| `packages/shared/goodmoneying_shared/sqlite_repository.py` | PostgreSQL과 같은 히트맵 계산 경로 구현 |
| `docs/contracts/api/openapi.yaml` | `DashboardSummary.realtimeCollectionHeatmap`, `RealtimeCollectionHeatmapCell`, `RealtimeCollectionHeatmapRow` 스키마 존재 |
| `apps/api/goodmoneying_api/schemas.py` | `RealtimeCollectionHeatmapCellResponse`, `RealtimeCollectionHeatmapRowResponse` 존재 |
| `apps/api/goodmoneying_api/service.py` | `realtime_heatmap_row_to_response()`로 backend View Model을 응답으로 변환 |
| `apps/web/src/components/Dashboard.tsx` | `실시간 정보 수집 현황` 카드와 `실시간 정보 수집 현황 히트맵` aria label 렌더링 |
| `tests/e2e/m1-operations.spec.ts` | `실시간 정보 수집 현황 히트맵` 표시 기대값 존재 |

## 자동 검증

| 명령 | 결과 | 메모 |
|---|---|---|
| `uv run pytest tests/contracts/test_api_contract.py tests/api/test_operations_api.py tests/shared/test_repository_behavior.py -q` | PASS | `33 passed, 1 warning` |
| `npm --workspace apps/web run test -- src/operationsDisplay.test.ts src/api.test.ts` | PASS | `2 files, 11 tests passed` |
| `rg -n "Status: Todo\|Status: In Progress\|Status: Ready for Verification\|Status: Blocked\|^- \[ \]" docs/Task --glob '*.md' --glob '!README.md' --glob '!템플릿.md'` | PASS | 미완료 Task 상태와 미완료 체크박스 없음 |
| `rg -n "[2]026-06-20-M3-T08\|[자]동 배포, 검증, 롤백\|I['']m using\|For agentic workers.*\[ \]" docs/Task docs/Test docs/History` | PASS | 이전 파일명, 부정확한 롤백 표현, 영어 작업 문구 드리프트 없음 |
| `git diff --check` | PASS | whitespace error 없음 |

## E2E 확인

이번 마감 감사에서는 로컬 `api`, `web`, `postgres`, worker가 모두 stopped 상태라 전체 E2E를 새로 실행하지 않았다. 대신 현재 E2E 테스트 파일이 `실시간 정보 수집 현황 히트맵` aria label을 검증하고 있음을 확인했고, 기존 통합 증적 `docs/Test/2026-06-20-M3-T04-운영-콘솔-관제-지표-확장-검증.md`에 Chromium E2E 1건 통과 기록이 있다.

## 결론

M3-T08 기능은 현재 코드와 계약 기준으로 구현되어 있고, 핵심 API/계약/저장소/Web 단위 검증은 통과했다. 별도 M3-T08 문서 상태와 체크박스가 `Todo`로 남아 있던 것은 문서 드리프트(drift)로 확인되어 Task 문서를 `Done`으로 마감했다.
