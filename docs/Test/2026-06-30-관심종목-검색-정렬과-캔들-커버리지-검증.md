# 2026-06-30-관심종목-검색-정렬과-캔들-커버리지-검증

Date: 2026-06-30
Related Task Summary: `docs/Task/M3.md`
Historical Task ID: `M3-T03-2026-06-30-002`
Environment: macOS, Python 3.14 가상환경(Virtual Environment), Node.js, FastAPI, React, Playwright Chromium, Browser plugin

## 검증 대상

- 관심종목 화면의 관심추가 항목 분리
- 헤더 정렬과 기본 24시간 거래대금 정렬
- 종목명/심볼 실시간 부분검색
- 등락률 기준 시각 KST 표시
- 실제 `source_candles` 저장 구간 기반 캔들 커버리지(Candle Coverage)

## 실행 명령

| 명령 | 결과 | 메모 |
|---|---|---|
| `uv run pytest tests/shared/test_repository_behavior.py -q` | 통과 | 8 passed |
| `npm test -- --run` | 통과 | Vitest 4 passed |
| `uv run pytest -q` | 통과 | 72 passed, 1 warning |
| `uv run ruff check .` | 통과 | 린트(Lint) 통과 |
| `uv run mypy apps/api apps/worker packages/shared tests` | 통과 | 타입 검사(Type Check) 통과 |
| `npm run build` | 통과 | TypeScript/Vite 빌드 통과 |
| `npm run e2e` | 통과 | Playwright E2E 1 passed |
| `git diff --check` | 통과 | 공백 오류 없음 |

## RED 확인

수정 전 다음 실패를 확인했다.

- React 테스트에서 `관심추가 항목`, 기본 거래대금 정렬, 검색 결과, 등락률 KST 기준 문구를 찾지 못함
- 저장소 테스트에서 첫 캔들 세그먼트 시작 시각이 실제 첫 캔들 시각이 아니라 수집 계획 시작 시각으로 반환됨
- E2E는 기존 고정 missing 세그먼트 기대값이 실제 저장 구간 기반 계산과 맞지 않아 수정 필요

## Browser QA

- URL: `http://127.0.0.1:15174/`
- 관심종목 화면:
  - `관심추가 항목` 표시 확인
  - `후보 유니버스 상위 100개` 표시 확인
  - 검색창 표시 확인
  - `24시간 거래대금 정렬`의 `aria-sort=descending` 확인
  - 검색어 `이더` 입력 시 `ETH / KRW`만 표시되고 `BTC / KRW`는 숨겨짐 확인
- 시장 리스트:
  - 등락률 헤더 표시 확인
  - `KST 기준` 표시 확인
  - 시장 행 렌더링 확인
- Console: error/warn 0건

## 결론

관심종목 화면은 선택 항목 분리, 검색, 정렬 요구사항을 만족한다. 캔들 커버리지는 실제 1분봉 저장 구간과 그 사이의 결측 구간을 반환한다.
