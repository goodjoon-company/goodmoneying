# P0-4 제품 기준 문서 재작성 검증

Date: 2026-07-10
Related Task: GitHub Issue #6
Environment: macOS 로컬 저장소, `codex/p0-4-post-mvp-correction`

## 검증 대상

- `docs/01_Product.md`가 제품 목표와 가까운 단계를 먼저 설명하는지 확인한다.
- M1~M3가 완료된 초기 제품 기준선이고 현재 범위가 초기 구현 이후 제품화(Post-MVP Productization)로 구분됐는지 확인한다.
- 운영 도구와 사용자 가치 화면의 경계가 명시됐는지 확인한다.
- `GM-PROD-001`부터 `GM-PROD-024`까지 현재 상태가 재분류됐는지 확인한다.
- M0~M11 장기 실행 계획이 `P0`, `P1`, `P2`, `P3`, `Later` 단계와 결정 게이트로 축소됐는지 확인한다.
- 기존 수집 품질 산식과 임계값이 의미 변경 없이 보존됐는지 확인한다.
- 런타임 코드와 API·DB 계약이 변경되지 않았는지 확인한다.

## 계획·스킬 점검

- 원천 PRD(Product Requirements Document): `docs/01_Product.md`
- PRD 단계: P0 현재 산출물 정리와 제품 기준 재정렬
- 실행 계획: GitHub Issue #6과 `docs/History/2026-07-04-제품-요구사항-로드맵-재정렬-계획.md` Task 4
- Milestone 상세화 미적용 사유: 단일 제품 기준 문서의 구조·상태·로드맵 재작성으로 범위와 수용 기준이 이미 구체적이다.
- 사용한 워크플로 스킬: `goodjoon-workflow:good-plan`, `goodjoon-workflow:good-review`, `goodjoon-workflow:good-sync`, `goodjoon-workflow:good-handoff`
- 코드 변경이 없어 `goodjoon-workflow:good-tdd`는 적용하지 않았다.
- 아키텍처·계약 변경은 후속 Issue #7 범위이므로 `goodjoon-workflow:good-spec`은 적용하지 않았다.
- 외부 스킬 점검: `external-skills: ok`. 상태 파일 `.goodjoon-workflow/external-skills-state.json` 생성을 확인했으며 현재 환경의 절대 경로를 담는 임시 증적이라 커밋 대상에서 제외했다.

## 실행 명령

| 명령 | 결과 | 메모 |
|---|---|---|
| `rg -n '^## (문서 역할\|제품 원칙\|제품 목표\|완료된 초기 제품 기준선\|다음 제품 단계\|현재 제품화 범위\|현재 범위 밖\|사용자 시나리오\|요구사항\|로드맵\|변경 규칙)$' docs/01_Product.md` | Pass | 권장 핵심 구조 11개 확인 |
| `rg -n 'M1~M3.*MVP.*완료|초기 구현 이후 제품화|현재 단계는 업비트 수집 MVP를 추가 구현하는 범위가 아니다' docs/01_Product.md` | Pass | 완료된 초기 기준선과 현재 제품화 단계 확인 |
| `test -z "$(rg -n '가까운 MVP|MVP 성공|^## MVP 범위|^## MVP 제외 범위|^### 핵심 MVP 시나리오' docs/01_Product.md)"` | Pass | MVP를 현재 구현 범위로 표현한 문구 0개 |
| `test "$(rg -o 'GM-PROD-[0-9]{3}' docs/01_Product.md \| sort -u \| wc -l \| tr -d ' ')" = '24'` | Pass | 요구사항 ID 24개 보존 |
| `test "$(rg -n '^\| GM-PROD-[0-9]{3} .*\| 완료 기준선 \| Implemented \|' docs/01_Product.md \| wc -l \| tr -d ' ')" = '8'` | Pass | 구현 요구사항 8개를 완료 기준선으로 분류 |
| `test -z "$(rg -n '\| Draft \|' docs/01_Product.md)"` | Pass | 개별 요구사항의 `Draft` 상태 0개 |
| `rg -n '\| (Implemented\|Accepted\|Candidate\|Deferred\|Removed) \|' docs/01_Product.md` | Pass | 상태 정의와 실제 상태 분류 확인 |
| `rg -n '^\| (P0\|P1\|P2\|P3\|Later) \|' docs/01_Product.md` | Pass | 가까운 단계 중심 로드맵 5개 확인 |
| `test -z "$(rg -n '^\| M(4\|5\|6\|7\|8\|9\|10\|11) \|' docs/01_Product.md)"` | Pass | 먼 미래 실행 단계 행 0개 |
| `rg -n 'quality_score = round\|freshness_threshold\|latency_threshold\|180초' docs/01_Product.md` | Pass | 기존 품질 점수 산식과 최신성 임계값 보존 |
| `git diff -- docs/contracts/api/openapi.yaml docs/contracts/db/schema.sql` | Pass | 출력 없음, 계약 변경 없음 |
| `git diff --name-only -- apps packages tests` | Pass | 출력 없음, 런타임 코드와 자동화 테스트 변경 없음 |
| `git diff --check` | Pass | 공백 오류 없음 |
| `python /Users/goodjoon/project/goodjoon/ai-dev-assets/goodjoon-workflow/scripts/goodjoon_workflow_gate.py external-skills --root /Users/goodjoon/project/goodjoon/ai-dev-assets/goodjoon-workflow --state .goodjoon-workflow/external-skills-state.json` | Pass | `external-skills: ok` |
| `python /Users/goodjoon/project/goodjoon/ai-dev-assets/goodjoon-workflow/scripts/goodjoon_workflow_gate.py bundle --root /Users/goodjoon/project/goodjoon/ai-dev-assets/goodjoon-workflow` | Pass | `bundle: ok` |

## 코드·문서 리뷰

- 요구사항 적합성: Issue #6의 제품 원칙, 완료된 초기 기준선, 현재 제품화 범위, 요구사항 상태, 가까운 로드맵을 모두 반영했다.
- 사용자 피드백 반영: M1~M3 완료 이후의 현재 단계를 MVP 구현으로 표현한 오류를 제거했다.
- 아키텍처 적합성: 현재 아키텍처나 모듈 책임을 변경하지 않았다. 제품 기준과 남은 아키텍처 문구의 정렬은 Issue #7로 유지한다.
- 계약 변경 여부: OpenAPI와 DB 스키마 변경이 없으며 메시지 계약도 변경하지 않았다.
- 기존 의미 보존: 수집 품질 산식·임계값과 구현된 호가 요약·화면 경계·Backfill 실행 규칙은 구조 단순화 과정에서 삭제하지 않고 제품 기준에 유지했다.
- 테스트 영향: 런타임 코드와 자동화 테스트 기대값 변경이 없어 기존 테스트 수정이 필요하지 않다.
- 보안·성능·운영 안정성: 실행 경로를 바꾸지 않는 문서 전용 변경이라 신규 영향이 없다.
- 추적성: Issue #6, Product, 이 Test, `docs/History/2026-07-10-P0-4-제품-기준-문서-재작성.md`를 연결했다.

## 종단 간 테스트와 QA

이번 작업은 런타임 코드, API·DB·메시지 계약, 사용자 화면 동작을 변경하지 않는다. 따라서 새 자동화 종단 간 테스트(E2E Test), 브라우저 QA, 디자인 QA는 실행하지 않았다. 현재 기능의 기존 자동화·브라우저 증적은 `docs/Test/`에 보존돼 있다.

## 미검증 항목

- 제품 기준과 아키텍처·업비트 수집 파이프라인·ADR 문구의 전체 정렬은 후속 GitHub Issue #7에서 검증한다.
- P0 전체의 회귀 테스트와 최종 인계는 GitHub Issue #8에서 수행한다.

## 결론

Issue #6의 문서 재작성 완료 기준을 충족한다. M1~M3는 완료된 초기 제품 기준선으로 고정됐고, 현재 범위는 투자 데이터 작업대의 제품화와 사용자 가치 검증으로 분리됐다. 요구사항 24개는 실제 상태로 분류됐고 먼 미래 단계는 결정 게이트로 축소됐다.
