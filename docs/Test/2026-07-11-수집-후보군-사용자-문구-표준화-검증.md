# 수집 후보군 사용자 문구 표준화 검증

Date: 2026-07-11
Related Task: GitHub Issue #11
Environment: macOS local, Python 3.14.3, Node.js/npm workspace, Playwright Chromium

## 검증 대상

- Backfill 관리 화면의 사용자 노출 문구가 `수집 후보군(Collection Candidate Pool)`을 사용하는지 확인한다.
- 저장소(Repository)와 워커(Worker)의 사용자 노출 오류·사유 메시지가 `수집 후보군`을 사용하는지 확인한다.
- `/v1/candidate-universe`, `CandidateUniverseResponse`, `in_universe` 같은 기존 API·스키마(schema)·식별자(identifier)는 유지한다.

## TDD 증적

| 단계 | 명령 | 결과 | 메모 |
|---|---|---|---|
| RED | `npm --workspace apps/web run test -- App.test.tsx targetBackfillWorkflow.test.ts` | 실패 | `수집 후보군 상위 100개`를 찾지 못해 실패했다. 기존 화면은 `후보 유니버스 상위 100개`를 표시했다. |
| RED | `uv run pytest tests/shared/test_repository_behavior.py::test_repository_rejects_new_active_target_outside_candidate_universe` | 실패 | 기대 정규식 `수집 후보군`과 실제 오류 메시지 `활성 수집 대상은 후보 유니버스 안에서만 선택할 수 있다.`가 달라 실패했다. |
| GREEN | `npm --workspace apps/web run test -- App.test.tsx targetBackfillWorkflow.test.ts` | 성공 | `2 passed`, `19 passed`. |
| GREEN | `uv run pytest tests/shared/test_repository_behavior.py::test_repository_rejects_new_active_target_outside_candidate_universe` | 성공 | `1 passed`. |

## 실행 명령

| 명령 | 결과 | 메모 |
|---|---|---|
| `python /Users/goodjoon/project/goodjoon/ai-dev-assets/goodjoon-workflow/scripts/goodjoon_workflow_gate.py external-skills --root /Users/goodjoon/project/goodjoon/ai-dev-assets/goodjoon-workflow --state .goodjoon-workflow/external-skills-state.json` | 성공 | `external-skills: ok`. 로컬 절대 경로가 들어간 상태 파일은 커밋하지 않고 결과만 기록했다. |
| `rg -n "후보 유니버스" apps packages scripts tests/e2e tests/shared tests/api --glob '!**/node_modules/**' --glob '!**/dist/**'` | 성공 | 출력 없음. 현재 소스와 활성 테스트 범위에 사용자 노출 기존 문구가 남지 않았다. |
| `uv run ruff check .` | 성공 | `All checks passed!` |
| `uv run mypy apps/api apps/worker packages/shared tests` | 성공 | `Success: no issues found in 37 source files` |
| `uv run pytest` | 성공 | `148 passed, 1 warning`. |
| `npm test` | 성공 | frontend `7 passed`, `38 passed`. |
| `npm run build` | 성공 | Vite production build 성공. |
| `git diff --check` | 성공 | 출력 없음. |
| `npm run e2e` | 성공 | Playwright Chromium `1 passed`; API `127.0.0.1:18000`, 웹 `127.0.0.1:15173` 시험 서버 종료 확인. |

## 수동 검증

- 별도 수동 검증은 수행하지 않았다. Backfill 관리 화면 진입과 주요 문구는 Playwright E2E에서 자동 검증했다.

## 미검증 항목

- 없음.

## 결론

Issue #11 범위의 사용자 화면과 사용자 노출 오류·사유 메시지는 `수집 후보군`으로 표준화됐다. 기존 API 경로·스키마·DB 계약은 변경하지 않았다.
