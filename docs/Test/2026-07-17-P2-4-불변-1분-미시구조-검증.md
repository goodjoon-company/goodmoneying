# P2-4 불변 1분 미시구조 검증 증적

날짜: 2026-07-17

## 검증 범위

- Decimal 공식, `BID=매수`·`ASK=매도`, 0 분모와 잘못된 호가 처리
- 원천 캔들 대사, 5단계 품질, 늦은 입력과 불변 부모 계보
- receipt·정규화 원천·더티 범위의 트랜잭션 원자성
- 저장 전용 REST, 고정 ID 상한 커서, 분석 WebSocket 청크·upsert·과거 refresh
- 빈 DB·기존 DB migration, 런타임 최소 권한, 브라우저 E2E

## 자동 검증 명령

```bash
uv run pytest -q tests/shared/test_versioned_microstructure.py
uv run pytest -q tests/contracts/test_p2_microstructure_contract.py
uv run pytest -q tests/api/test_operations_api.py
uv run pytest -q tests/e2e/test_live_postgres_microstructure.py
uv run mypy apps/api apps/worker packages/shared tests
uv run ruff check .
npm test
npm run build
npm run e2e
GOODMONEYING_UPDATE_DB_SNAPSHOT=1 tests/e2e/run_dbmate_migration_e2e.sh
git diff --check
```

## 최종 결과

커밋 직전의 동일 작업 트리에서 다음 결과를 새로 확인했다.

- 전체 Python 회귀: `574 passed, 81 skipped`
- PostgreSQL 마이그레이션·실DB E2E: `80 passed`, migration 13개, API `200`, schema snapshot 동일
- 웹 단위 테스트: `23 files, 140 passed`
- 웹 프로덕션 빌드: 성공
- Playwright 브라우저 E2E: `17 passed`, 종료 뒤 API·웹 시험 서버 정리 확인
- mypy: `99 source files`, 오류 0건
- Ruff·`git diff --check`: 통과
- 독립 요구사항·아키텍처·계약 코드 리뷰: Critical 0건, Important 0건

실DB E2E는 정상 10단계 기본 호가와 BID·ASK 체결의 캔들 대사 `ready` 값, 연결 시작·종료 부분 분, 장애·재연결 공백, A→B→A 부모 계보와 `asOf`, 선행 프런티어 대기 중 재시도 예산 보존, 만료 임대 회수, 늦은 물질화가 최초 REST 커서 상한을 넘지 못하는 동작을 포함한다.
