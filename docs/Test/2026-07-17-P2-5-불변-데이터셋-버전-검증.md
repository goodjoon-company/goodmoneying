# P2-5 불변 데이터셋 버전 검증 증적

날짜: 2026-07-17

## 검증 범위

- 수락 프런티어 고정, 멱등 충돌과 A→B→A 정정의 서로 다른 내용 해시
- typed member의 자연키·내용 해시·`knowledge_at`·`source_as_of` 원천 일치
- 직접 일봉과 1분 집계 폴백, 시장 상태·품질 격리, 5단계 coverage와 `no_trade`
- 봉인 뒤 root·모든 child 변이 차단, 동시 내용 주소 재사용
- heartbeat·세대·만료 fencing, 재시도·dead-letter와 안정 API 오류 코드
- 4,096행 증분 정규 해시, 대리키 독립성, 10,001행 bounded-memory 게시
- 주·월 비정렬 원천 폴백의 UTC 반개방 범위
- 빈 DB·기존 DB migration, schema snapshot, REST 런타임과 프런트엔드 회귀

## 자동 검증 명령

```bash
uv run pytest -q
uv run mypy apps/api apps/worker packages/shared tests
uv run ruff check .
npm test
npm run build
npm run e2e
GOODMONEYING_UPDATE_DB_SNAPSHOT=1 tests/e2e/run_dbmate_migration_e2e.sh
tests/e2e/run_dbmate_migration_e2e.sh
git diff --check
```

## 최종 결과

커밋 직전의 동일 작업 트리에서 다음 결과를 새로 확인했다.

- 전체 Python 회귀: `647 passed, 119 skipped`
- PostgreSQL 마이그레이션·실DB E2E: `118 passed`, migration 14개, API `200`, schema snapshot 동일
- 10,001행 게시 성능: 단독 `1.58초`, 64 MiB 미만, 저장 행 수·참조 manifest·시계열 hash 동일
- 웹 단위 테스트: `23 files, 140 passed`
- 웹 프로덕션 빌드: 성공
- Playwright 브라우저 E2E: `17 passed`, 종료 뒤 API·웹 시험 서버 정리 확인
- mypy: `109 source files`, 오류 0건
- Ruff·`git diff --check`: 통과
- 리뷰 수정 뒤 `dataset_version_store.py` 문법 컴파일(py_compile), 전체 Python 회귀, 실DB E2E와 웹 E2E를 다시 확인했다.

실DB 검증은 수락 뒤 늦은 정정·시장 상태 변경 배제, 모든 child 봉인 차단, 복수 정의 series, 계산 버전 불일치, 일봉 혼합, 타 품질 종류 격리, retry-wait·dead-letter, 정확한 원천 내용·인과 시각 trigger와 대용량 스트리밍 게시를 포함한다.
