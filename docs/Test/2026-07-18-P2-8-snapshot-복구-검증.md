# P2-8 snapshot 복구 검증

Date: 2026-07-18
Related Task: [P2](../Task/P2.md), GitHub Issue #29
Environment: macOS local worktree, Python uv, Node/Vitest, FastAPI TestClient

## 검증 대상

- `/v1/realtime/analysis/snapshot` REST snapshot과 서명 cursor
- REST snapshot cursor를 사용한 WebSocket 재구독의 초기 snapshot 재전송 생략
- cursor snapshot version 불일치 거부
- `snapshot_required`와 `slow_consumer` 이후 브라우저 REST snapshot 자동 복구
- OpenAPI와 내부 realtime stream JSON Schema 계약

## 실행 명령

| 명령 | 결과 | 메모 |
|---|---|---|
| `uv run pytest tests/api/test_p2_realtime_stream_api.py -q` | RED 확인 | snapshot endpoint 404, REST cursor 재구독 cursor 없음 |
| `npm --workspace apps/web run test -- src/useRealtimeAnalysis.test.tsx` | RED 확인 | snapshot_required 뒤 REST snapshot 호출 없음 |
| `uv run pytest tests/shared/test_realtime_stream.py tests/contracts/test_p2_realtime_stream_contract.py -q` | RED 확인 | snapshot version 불일치 cursor resume 미거부 |
| `uv run pytest tests/api/test_p2_realtime_stream_api.py tests/shared/test_realtime_stream.py tests/contracts/test_p2_realtime_stream_contract.py tests/contracts/test_api_contract.py -q` | PASS | `26 passed, 1 warning` |
| `npm --workspace apps/web run test -- src/useRealtimeAnalysis.test.tsx src/realtimeStream.test.ts` | PASS | `2 passed`, `16 passed` |
| `uv run ruff check apps/api/goodmoneying_api/main.py packages/shared/goodmoneying_shared/realtime_stream.py tests/api/test_p2_realtime_stream_api.py tests/shared/test_realtime_stream.py tests/contracts/test_p2_realtime_stream_contract.py && uv run mypy apps/api apps/worker packages/shared tests` | PASS | ruff 통과, mypy `113 source files` 통과 |
| `uv run pytest tests/contracts/test_api_contract.py tests/contracts/test_p2_realtime_stream_contract.py -q` | PASS | `14 passed` |
| `ruby -e 'require "yaml"; c=YAML.load_file("docs/contracts/api/openapi.yaml"); puts "openapi paths=#{c.fetch("paths").size} schemas=#{c.fetch("components").fetch("schemas").size}"'` | PASS | `openapi paths=39 schemas=87` |
| `git diff --check && uv run ruff check . && uv run mypy apps/api apps/worker packages/shared tests` | PASS | diff whitespace 없음, ruff 통과, mypy `113 source files` 통과 |
| `npm test` | PASS | `26 passed`, `159 passed` |
| `npm --workspace apps/web run build` | PASS | Vite chunk size warning만 존재 |
| `uv run pytest -q` | PASS | `667 passed, 119 skipped, 1 warning` |
| `npm run e2e` | PASS | `19 passed`, API 시험 서버와 웹 시험 서버 종료 확인 |
| `tests/e2e/run_dbmate_migration_e2e.sh` | PASS | `118 passed`, `versions=14`, `data_rows=1`, `timezone=UTC`, `API=200`, `snapshot=동일`, `집계상태=동일` |
| 코드 리뷰 재검토 | PASS | Critical/Important 잔여 지적사항 없음 |

## 수동 검증

- 없음. WebSocket 지연 네트워크 자체는 TestClient에서 안정적으로 재현하지 않고, 서버 handler의 timeout guard와 `slow_consumer` envelope 경로를 구조 테스트와 계약 테스트로 고정했다.

## 미검증 항목

- 원격 CI는 push 이후 추가 기록한다.

## 결론

P2-8 표적 구현과 로컬 전체 회귀·E2E는 통과했다. 전체 완료 선언은 원격 CI 통과 뒤 갱신한다.
