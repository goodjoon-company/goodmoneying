# 2026-07-18-P4-1-백테스트-엔진-코어-검증

Date: 2026-07-18
Related Task: GitHub Issue #31, P4-1
Environment: macOS local worktree, Python 3.14, uv, Node/npm

## 검증 대상

- `backtest-core-v1` 순수 백테스트 엔진
- P4 백테스트 엔진 계약(Backtest Engine Contract)
- 결정론적 `input_hash`/`result_hash`
- Decimal 기반 수수료·슬리피지(slippage)·지연(latency)·부분 체결(partial fill)
- golden replay 신호 동등성

## RED 증적

| 명령 | 결과 | 메모 |
|---|---|---|
| `uv run pytest tests/shared/test_backtest_engine.py -q` | 실패 | `ModuleNotFoundError: No module named 'goodmoneying_shared.backtest_engine'` |
| `uv run pytest tests/contracts/test_p4_backtest_contract.py -q` | 실패 | `docs/contracts/backtest-engine.md` 부재, 도메인 문서의 `golden replay` 요구 부재 |
| `uv run pytest tests/shared/test_backtest_engine.py -q` | 실패 | 추가 RED: 지연(latency) 이후 체결 가능한 사건이 없는데 같은 시각 사건으로 체결됨 |
| `uv run pytest tests/shared/test_backtest_engine.py::test_P4_1_float_입력은_Decimal_손실로_거부한다 -q` | 실패 | 추가 RED: float 입력이 `decimal_required` 실패 결과가 아니라 `TypeError`로 중단됨 |

## GREEN 및 회귀 검증

| 명령 | 결과 | 메모 |
|---|---|---|
| `uv run pytest tests/shared/test_backtest_engine.py::test_P4_1_float_입력은_Decimal_손실로_거부한다 -q` | `1 passed in 0.01s` | Decimal 손실 방지 |
| `uv run pytest tests/shared/test_backtest_engine.py tests/contracts/test_p4_backtest_contract.py tests/e2e/test_p4_backtest_engine_e2e.py -q` | `10 passed in 0.02s` | 단위·계약·순수 엔진 E2E |
| `uv run ruff check packages/shared/goodmoneying_shared/backtest_engine.py tests/shared/test_backtest_engine.py tests/contracts/test_p4_backtest_contract.py tests/e2e/test_p4_backtest_engine_e2e.py` | `All checks passed!` | 변경 범위 lint |
| `uv run mypy packages/shared tests/shared/test_backtest_engine.py tests/contracts/test_p4_backtest_contract.py tests/e2e/test_p4_backtest_engine_e2e.py` | `Success: no issues found in 26 source files` | 변경 범위 strict type check |
| `uv run pytest tests/shared tests/contracts tests/e2e/test_p4_backtest_engine_e2e.py -q` | `300 passed in 35.91s` | shared·contract 회귀와 P4-1 E2E |
| `uv run pytest -q` | `685 passed, 122 skipped, 1 warning in 56.59s` | 전체 Python 회귀 |
| `uv run ruff check .` | `All checks passed!` | 저장소 전체 lint |
| `uv run mypy apps/api apps/worker packages/shared tests` | `Success: no issues found in 123 source files` | 저장소 Python strict type check |
| `npm test -- --runInBand` | `27 files passed, 168 tests passed` | Web 회귀. npm의 `--runInBand` unknown config warning은 실패 아님 |
| `npm run build` | 성공 | Vite chunk size warning은 기존 번들 크기 경고 |
| `uv run python /Users/goodjoon/.codex/plugins/cache/goodjoon-codex-tools/goodjoon-workflow/0.3.0/workflow/scripts/goodjoon_workflow_gate.py external-skills --root /Users/goodjoon/.codex/plugins/cache/goodjoon-codex-tools/goodjoon-workflow/0.3.0/workflow --state .goodjoon-workflow/external-skills-state.json` | `external-skills: ok` | goodjoon-workflow 외부 skill 상태 게이트 |
| `git diff --check` | 통과 | 공백 오류 없음 |

## 수동 검증

- 코드 리뷰에서 P4-1 범위를 순수 공유 엔진으로 제한했는지 확인했다.
- 실패 사례 검토에서 Decimal 손실 입력이 산술 예외가 아니라 실패 결과로 수렴하는지 추가 테스트로 보강했다.
- DB/API/UI 변경이 없는 조각이라 운영 데이터·비밀·실거래 경로를 건드리지 않았음을 확인했다.

## 미검증 항목

- Backtest Worker, DB migration, REST·WebSocket 진행 이벤트, Backtest Lab 브라우저 E2E는 P4 후속 조각 범위다. P4-1은 순수 엔진 E2E(`tests/e2e/test_p4_backtest_engine_e2e.py`)로 자동화했다.
- walk-forward, sensitivity, bootstrap 산출물은 P4 후속 조각에서 계약과 테스트를 추가한다.

## 결론

P4-1 백테스트 엔진 코어는 결정론적 사건 재생과 체결 가정 기록 요구를 만족한다. 전체 P4 완료는 아니며, Issue #31의 후속 수직 조각으로 Worker·DB·API·UI·E2E를 계속 구현해야 한다.
