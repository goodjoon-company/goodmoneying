# 2026-07-18-P4-2-백테스트-영속화-검증

Date: 2026-07-18
Related Task: GitHub Issue #31, P4-2
Environment: macOS local worktree, Python 3.14, uv, Docker, PostgreSQL 17.10

## 검증 대상

- `20260718000200_p4_backtest_runs.sql`
- `PostgresBacktestStore`
- `backtest_runs`, `backtest_trades`, `backtest_equity_points`, `backtest_metrics`, `backtest_artifacts`
- published 전략 버전과 sealed 데이터셋 버전 입력 제약
- terminal run과 결과 행 append-only 봉인
- dbmate schema snapshot

## RED 증적

| 명령 | 결과 | 메모 |
|---|---|---|
| `uv run pytest tests/contracts/test_p4_backtest_db_contract.py -q` | 실패 | `docs/contracts/db/migrations/20260718000200_p4_backtest_runs.sql` 부재 |
| `GOODMONEYING_UPDATE_DB_SNAPSHOT=1 tests/e2e/run_dbmate_migration_e2e.sh` | 실패 | 저장 직후 같은 트랜잭션의 run을 별도 연결로 조회해 `saved is not None` assert 실패 |
| `GOODMONEYING_UPDATE_DB_SNAPSHOT=1 tests/e2e/run_dbmate_migration_e2e.sh` | 실패 | 멱등 replay 호출에서 artifact 생략으로 request hash가 달라져 `BacktestIdempotencyConflictError` 발생 |

## GREEN 및 회귀 검증

| 명령 | 결과 | 메모 |
|---|---|---|
| `uv run pytest tests/contracts/test_p4_backtest_db_contract.py tests/contracts/test_p4_backtest_persistence_contract.py -q` | `5 passed in 0.01s` | P4-2 DB·문서 계약 |
| `uv run pytest tests/contracts/test_p4_backtest_db_contract.py tests/contracts/test_p4_backtest_persistence_contract.py tests/e2e/test_live_postgres_backtest_runs.py -q` | `5 passed, 1 skipped in 0.07s` | live 미활성 non-live 검증 |
| `uv run ruff check packages/shared/goodmoneying_shared/backtest_store.py tests/contracts/test_p4_backtest_db_contract.py tests/contracts/test_p4_backtest_persistence_contract.py tests/e2e/test_live_postgres_backtest_runs.py` | `All checks passed!` | P4-2 변경 범위 lint |
| `uv run mypy packages/shared/goodmoneying_shared/backtest_store.py tests/contracts/test_p4_backtest_db_contract.py tests/contracts/test_p4_backtest_persistence_contract.py tests/e2e/test_live_postgres_backtest_runs.py` | `Success: no issues found in 4 source files` | P4-2 변경 범위 strict type check |
| `GOODMONEYING_UPDATE_DB_SNAPSHOT=1 tests/e2e/run_dbmate_migration_e2e.sh` | `122 passed in 34.84s`, `versions=16`, `timezone=UTC`, `API=200`, `snapshot=동일` | 빈 PostgreSQL migration, schema dump 갱신, live PostgreSQL E2E |
| `uv run pytest -q` | `690 passed, 123 skipped, 1 warning in 58.92s` | 전체 Python 회귀 |
| `uv run ruff check .` | `All checks passed!` | 저장소 전체 lint |
| `uv run mypy apps/api apps/worker packages/shared tests` | `Success: no issues found in 127 source files` | 저장소 Python strict type check |

## 수동 검증

- `backtest_runs`가 strategy graph hash와 dataset content hash를 FK로 함께 고정하는지 migration을 검토했다.
- terminal run 업데이트와 child 결과 테이블 변경이 trigger로 거부되는지 live PostgreSQL E2E로 확인했다.

## 미검증 항목

- Backtest Worker 임대·재시도·artifact storage 정책은 다음 P4 조각에서 구현한다.
- REST·WebSocket 진행 이벤트와 Backtest Lab 브라우저 E2E는 다음 P4 조각에서 구현한다.

## 결론

P4-2 백테스트 영속화 계층은 DB 계약과 PostgreSQL 저장소 소비자 관점에서 통과했다. 전체 P4 완료는 아니며 Worker/API/UI 흐름을 이어서 구현해야 한다.
