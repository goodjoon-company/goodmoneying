# 2026-07-18 P4-5 백테스트 Worker 검증

## 범위

- Backtest Worker 임대(lease)·재시도(retry)·dead-letter 상태 전이
- `lease_generation` 기반 fencing
- 완료 결과와 artifact의 같은 transaction 저장
- dbmate migration과 schema snapshot

## 검증 결과

| 명령 | 결과 | 근거 |
|---|---|---|
| `uv run pytest tests/worker/test_backtest_worker.py tests/shared/test_backtest_worker_lease.py tests/contracts/test_p4_backtest_persistence_contract.py -q` | PASS | `17 passed in 0.08s` |
| `uv run ruff check packages/shared/goodmoneying_shared/backtest_store.py apps/worker/goodmoneying_worker/backtest_worker.py tests/worker/test_backtest_worker.py tests/shared/test_backtest_worker_lease.py tests/e2e/test_live_postgres_backtest_runs.py tests/contracts/test_p4_backtest_persistence_contract.py` | PASS | `All checks passed!` |
| `uv run mypy packages/shared/goodmoneying_shared/backtest_store.py apps/worker/goodmoneying_worker/backtest_worker.py tests/worker/test_backtest_worker.py tests/shared/test_backtest_worker_lease.py tests/e2e/test_live_postgres_backtest_runs.py` | PASS | `Success: no issues found in 5 source files` |
| `GOODMONEYING_UPDATE_DB_SNAPSHOT=1 tests/e2e/run_dbmate_migration_e2e.sh` | PASS | `123 passed in 39.57s`, `versions=17`, `timezone=UTC`, `API=200`, `snapshot=동일`, `집계상태=동일` |
| `uv run ruff check .` | PASS | `All checks passed!` |
| `uv run mypy apps/api apps/worker packages/shared tests` | PASS | `Success: no issues found in 133 source files` |
| `uv run pytest -q` | PASS | `714 passed, 124 skipped, 1 warning in 59.05s` |
| `npm test` | PASS | `28 passed`, `175 passed` |
| `npm run build` | PASS | Vite build 성공, 기존 chunk size 경고만 출력 |
| `npx playwright test tests/e2e/p4-backtest-lab.spec.ts` | PASS | `1 passed (3.2s)` |
| `git diff --check` | PASS | 출력 없음 |

## RED/GREEN 기록

- RED: `BacktestLeaseLostError`, `claim_next_run`, `complete_claimed_run`, `fail_claimed_run`, `BacktestWorker`가 없어 worker·lease 테스트가 import 단계에서 실패했다.
- GREEN: `backtest_runs` worker lease migration, `PostgresBacktestStore` claim/complete/fail, `BacktestWorker` core를 추가했다.
- 보정: live PostgreSQL 테스트에서 dataset fixture가 동일 `content_hash`를 재사용해 unique 제약과 충돌했다. key 기반 SHA-256 content hash로 수정했다.

## 남은 후속 범위

- 실행 생성 API와 권한 정책
- strategy/dataset replay materialization
- 외부 object storage artifact store와 orphan cleanup
- 대용량 trade/equity pagination과 WebSocket 진행 이벤트
