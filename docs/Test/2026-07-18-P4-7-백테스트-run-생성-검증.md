# 2026-07-18 P4-7 백테스트 run 생성 검증

## 범위

- `POST /v1/backtest-runs` 실행 생성 API
- published 전략 version과 sealed 데이터셋 version 입력 검증
- strategy/dataset materialization과 engine input payload 기반 `input_hash` 생성
- `backtest_runs.input_payload` 저장과 Worker claim 후 engine complete hash 일치
- idempotency key 재생·충돌 처리
- Web client `createBacktestRun`

## RED

- `uv run pytest tests/contracts/test_p4_backtest_api_contract.py tests/api/test_backtest_runs_api.py tests/shared/test_backtest_result_pagination.py -q`
  - 실패: `BacktestInputNotReadyError`와 `create_run` 미구현으로 collection error
- `npm --workspace apps/web run test -- api.test.ts`
  - 실패: `createBacktestRun is not a function`
- `uv run pytest tests/api/test_backtest_runs_api.py::test_백테스트_run_생성은_체결_모델_범위를_검증한다 tests/contracts/test_p4_backtest_api_contract.py::test_P4_7_OpenAPI는_백테스트_run_생성_계약을_노출한다 -q`
  - 실패: execution model의 `feeRate`, `slippageBps`, `maxParticipationRate` 범위 검증 누락으로 `5 failed`

## Targeted GREEN

- `uv run pytest tests/contracts/test_api_contract.py tests/contracts/test_p4_backtest_api_contract.py tests/api/test_backtest_runs_api.py tests/shared/test_backtest_result_pagination.py tests/e2e/test_live_postgres_backtest_runs.py -q`
  - 결과: `37 passed, 3 skipped, 1 warning`
- `uv run mypy apps/api apps/worker packages/shared tests`
  - 결과: `Success: no issues found in 134 source files`
- `npm --workspace apps/web run test -- api.test.ts`
  - 결과: `1 passed`, `14 passed`

## 전체 검증

- `uv run ruff check .`
  - 결과: `All checks passed!`
- `uv run mypy apps/api apps/worker packages/shared tests`
  - 결과: `Success: no issues found in 134 source files`
- `uv run pytest -q`
  - 결과: `734 passed, 125 skipped, 1 warning`
- `GOODMONEYING_UPDATE_DB_SNAPSHOT=1 tests/e2e/run_dbmate_migration_e2e.sh`
  - 결과: `124 passed`, `versions=18`, `data_rows=1`, `timezone=UTC`, `API=200`, `snapshot=동일`, `집계상태=동일`
- `npm test`
  - 결과: `28 passed`, `177 passed`
- `npm run build`
  - 결과: 성공, 기존 Vite chunk size warning만 존재
- `npx playwright test tests/e2e/p4-backtest-lab.spec.ts`
  - 결과: `1 passed`
- `git diff --check`
  - 결과: 출력 없음, 통과

## 원격 검증

원격 CI 결과는 push 후 이 문서에 추가한다.
