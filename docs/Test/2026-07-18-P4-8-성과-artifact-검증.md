# 2026-07-18 P4-8 백테스트 성과 artifact 검증

## 범위

- walk-forward, sensitivity, bootstrap 성과 artifact 입력 타입
- `build_performance_artifacts()` 결정론적 metadata와 `contentHash`
- Backtest Store artifact 입력 형태(`artifactType`, `contentHash`, `mediaType`, `storageUri`, `metadata`)

## RED

- `uv run pytest tests/shared/test_backtest_engine.py::test_P4_8_성과_artifact는_결정론적_hash와_store_payload를_생성한다 -q`
  - 실패: `BootstrapSample`, `SensitivityPoint`, `WalkForwardFold`, `build_performance_artifacts` 미구현 import error

## Targeted GREEN

- `uv run pytest tests/shared/test_backtest_engine.py::test_P4_8_성과_artifact는_결정론적_hash와_store_payload를_생성한다 -q`
  - 결과: `1 passed`
- `uv run pytest tests/shared/test_backtest_engine.py tests/contracts/test_p4_backtest_contract.py -q`
  - 결과: `11 passed`
- `uv run mypy packages/shared tests/shared/test_backtest_engine.py tests/contracts/test_p4_backtest_contract.py`
  - 결과: `Success: no issues found in 26 source files`

## 전체 검증

- `uv run ruff check .`
  - 결과: `All checks passed!`
- `uv run mypy apps/api apps/worker packages/shared tests`
  - 결과: `Success: no issues found in 134 source files`
- `uv run pytest -q`
  - 결과: `736 passed, 125 skipped, 1 warning`
- `npm test`
  - 결과: `28 passed`, `177 passed`
- `npm run build`
  - 결과: 성공, 기존 Vite chunk size warning만 존재
- `npx playwright test tests/e2e/p4-backtest-lab.spec.ts`
  - 결과: `1 passed`
- `git diff --check`
  - 결과: 출력 없음, 통과

## 원격 검증

원격 CI 결과는 push 후 이 문서에 추가한다.
