# 2026-07-18 P4-9 백테스트 진행 WebSocket 검증

## 범위

- `ws://<api-host>/v1/backtest-runs/{backtestRunId}/progress` 진행 snapshot 계약
- 현재 `BacktestRunSummary` 기반 `backtest.progress` 메시지
- 없는 run의 안정 오류(`backtest.error`, `BACKTEST_RUN_NOT_FOUND`)
- Web client의 `backtestProgressWebSocketUrl()` base path 보존

## RED

- `uv run pytest tests/api/test_backtest_runs_api.py::test_백테스트_progress_websocket은_현재_run_진행_snapshot을_전송한다 tests/api/test_backtest_runs_api.py::test_백테스트_progress_websocket은_없는_run을_안정된_오류로_전송한다 -q`
  - 실패: WebSocket route 미구현으로 연결 시 `WebSocketDisconnect`

## Targeted GREEN

- `uv run pytest tests/api/test_backtest_runs_api.py::test_백테스트_progress_websocket은_현재_run_진행_snapshot을_전송한다 tests/api/test_backtest_runs_api.py::test_백테스트_progress_websocket은_없는_run을_안정된_오류로_전송한다 -q`
  - 결과: `2 passed, 1 warning`
- `uv run pytest tests/api/test_backtest_runs_api.py::test_백테스트_progress_websocket은_현재_run_진행_snapshot을_전송한다 tests/api/test_backtest_runs_api.py::test_백테스트_progress_websocket은_없는_run을_안정된_오류로_전송한다 tests/contracts/test_p4_backtest_contract.py::test_P4_9_백테스트_진행_WebSocket_계약을_명시한다 -q`
  - 결과: `3 passed, 1 warning`
- `npm test -- --run src/api.test.ts -t "백테스트 진행 WebSocket URL은 API base path를 보존한다"`
  - 결과: `1 passed`, 전체 `src/api.test.ts` `15 passed`

## 전체 검증

- `uv run ruff check .`
  - 결과: `All checks passed!`
- `uv run mypy apps/api apps/worker packages/shared tests`
  - 결과: `Success: no issues found in 134 source files`
- `uv run pytest -q`
  - 결과: `739 passed, 125 skipped, 1 warning`
- `npm test`
  - 결과: `28 passed`, `178 passed`
- `npm run build`
  - 결과: 성공, 기존 Vite chunk size warning만 존재
- `npx playwright test tests/e2e/p4-backtest-lab.spec.ts`
  - 결과: `1 passed`
- `git diff --check`
  - 결과: 출력 없음, 통과

## 원격 검증

원격 CI 결과는 push 후 이 문서에 추가한다.
