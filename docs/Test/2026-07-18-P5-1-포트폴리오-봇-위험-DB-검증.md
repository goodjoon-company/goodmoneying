# 2026-07-18 P5-1 포트폴리오·봇·위험 DB 검증

## 범위

- portfolio, policy, capital allocation DB 계약
- paper/shadow bot instance와 상태 전이 감사 DB 계약
- order intent, simulated exchange order, fill, position projection DB 계약
- risk limit/event와 kill switch DB 계약
- append-only audit/fill 보호와 P6/live 범위 차단

## RED

- `uv run pytest tests/contracts/test_p5_portfolio_bot_risk_contract.py -q`
  - 결과: `4 failed`
  - 원인: `20260718000500_p5_portfolio_bot_risk.sql` 없음, P5 제품 상태 문구 미정합

## Targeted GREEN

- `uv run pytest tests/contracts/test_p5_portfolio_bot_risk_contract.py -q`
  - 결과: `4 passed`
- `uv run pytest tests/contracts/test_p5_portfolio_bot_risk_contract.py tests/e2e/test_live_postgres_portfolio_bot_risk.py -q`
  - 결과: `4 passed, 1 skipped`

## DB migration E2E

- `GOODMONEYING_UPDATE_DB_SNAPSHOT=1 tests/e2e/run_dbmate_migration_e2e.sh`
  - 결과: `versions=19`, `data_rows=1`, `timezone=UTC`, `API=200`, `snapshot=동일`, `집계상태=동일`
  - live PostgreSQL 테스트: `124 passed in 38.65s`

## 전체 검증

- `uv run ruff check .`
  - 결과: `All checks passed!`
- `uv run mypy apps/api apps/worker packages/shared tests`
  - 결과: `Success: no issues found in 136 source files`
- `uv run pytest -q`
  - 결과: `743 passed, 126 skipped, 1 warning`
- `npm test`
  - 결과: `28 passed`, `178 passed`
- `npm run build`
  - 결과: 성공, 기존 Vite chunk size warning만 존재
- `npx playwright test tests/e2e/p4-backtest-lab.spec.ts`
  - 결과: `1 passed`
- `git diff --check`
  - 결과: 출력 없음, 통과

## 원격 검증

원격 CI 결과는 push 후 Issue #32에 기록한다.
