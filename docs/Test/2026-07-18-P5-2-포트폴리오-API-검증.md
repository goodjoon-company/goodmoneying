# 2026-07-18 P5-2 포트폴리오 API 검증

## 범위

- `POST /v1/portfolios` 멱등 명령 API
- `GET /v1/portfolios` owner 문맥 cursor 목록 API
- `PostgresPortfolioBotStore` 생성·목록 PostgreSQL 영속화
- `20260718000600_p5_portfolio_api_commands.sql` migration과 `schema.sql` snapshot

## RED

```bash
uv run pytest tests/api/test_portfolio_bot_api.py -q
```

결과: `ModuleNotFoundError: No module named 'goodmoneying_shared.portfolio_bot_store'`

```bash
uv run pytest tests/contracts/test_p5_portfolio_api_contract.py -q
```

결과: `1 failed, 1 passed`; OpenAPI 포트폴리오 계약 미존재.

## GREEN

```bash
uv run pytest tests/api/test_portfolio_bot_api.py tests/contracts/test_p5_portfolio_api_contract.py -q
```

결과: `5 passed, 1 warning`

```bash
GOODMONEYING_UPDATE_DB_SNAPSHOT=1 tests/e2e/run_dbmate_migration_e2e.sh
```

결과: `dbmate 마이그레이션 E2E 통과: versions=20 data_rows=1 timezone=UTC API=200 snapshot=동일 집계상태=동일`, live PostgreSQL 회귀 `124 passed in 38.66s`

```bash
GOODMONEYING_LIVE_POSTGRES_TEST=1 GOODMONEYING_DATABASE_URL="postgresql://goodmoneying:goodmoneying-e2e@127.0.0.1:<임시포트>/goodmoneying?sslmode=disable" uv run pytest tests/e2e/test_live_postgres_portfolio_api.py -q
```

결과: 임시 PostgreSQL에 전체 migration 적용 후 `2 passed in 0.12s`

## 아직 후속 범위

- paper execution worker
- risk evaluation과 kill switch 차단 transaction
- reconciliation과 position projection 갱신
- Bot Workshop UI와 E2E
